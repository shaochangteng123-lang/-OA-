# 06 - 路由与布局模块

## 模块概述

路由与布局模块负责应用的页面路由管理、导航守卫、页面布局框架和权限控制。

## 主要功能

### 1. 路由管理

- 页面路由配置
- 懒加载优化
- 路由元信息管理
- 动态路由生成

### 2. 导航守卫

- 登录状态检查
- 权限验证
- 页面访问控制
- 重定向逻辑

### 3. 布局系统

- 主布局框架
- 侧边栏导航
- 顶部导航栏
- 面包屑导航

### 4. 页面切换

- 路由过渡动画
- 页面加载状态
- 错误页面处理

## 核心文件

### 路由配置

- [src/router/index.ts](../src/router/index.ts) - 路由配置主文件

### 布局组件

- [src/layouts/MainLayout.vue](../src/layouts/MainLayout.vue) - 主布局框架
- [src/layouts/components/](../src/layouts/components/) - 布局子组件

## 羽隶经营看板布局

- `/boss-dashboard`（羽隶经营看板）仅允许BOSS、管理员和超级管理员访问。
- BOSS不需要填写入职资料，登录后由导航守卫直接进入经营看板。
- BOSS侧边栏只展示经营管理和个人设置，不展示普通业务操作区与审批中心。
- BOSS没有业务待办，主布局不为BOSS启动待办轮询。
- BOSS直接访问入职页或普通业务页面时自动返回经营看板。
- 看板首屏采用浅色、紧凑的经营驾驶舱布局，顶部集中展示名称、所选财务周期、数据质量、更新时间、当前状态摘要和周期选择，不再使用大面积深色首屏。
- 首屏下方提供“经营概览”“财务分析”“人员与日志”“项目与风险”四个互斥分区；任一时刻只渲染当前选择的分区，不再把所有图表、人员、日志、项目和风险纵向堆叠在同一个总览中。
- 按月时选择一个自然月，按年时选择一个自然年；周期控件只改变财务指标、趋势和报销明细。人员、日志、项目和风险分区明确展示当前实时快照，不冒充所选历史周期。
- 看板保留手动刷新、全屏查看、最后更新时间、同步倒计时和每 30 秒静默自动刷新。

## 路由配置

### 路由结构

```typescript
const routes = [
  {
    path: "/",
    component: MainLayout,
    redirect: "/home",
    children: [
      {
        path: "/home",
        name: "Home",
        component: () => import("@/views/Home.vue"),
        meta: {
          title: "工作日志",
          requiresAuth: true,
          permission: "worklogs.view",
        },
      },
      {
        path: "/calendar",
        name: "Calendar",
        component: () => import("@/views/Calendar.vue"),
        meta: {
          title: "日历",
          requiresAuth: true,
          permission: "calendar.view",
        },
      },
      // ... 其他路由
    ],
  },
  {
    path: "/login",
    name: "Login",
    component: () => import("@/views/Login.vue"),
    meta: { title: "登录" },
  },
];
```

### 路由元信息

```typescript
interface RouteMeta {
  title?: string; // 页面标题
  requiresAuth?: boolean; // 是否需要登录
  permission?: string; // 所需权限
  roles?: string[]; // 允许的角色
  icon?: string; // 菜单图标
  hidden?: boolean; // 是否在菜单中隐藏
  breadcrumb?: boolean; // 是否显示面包屑
}
```

## 导航守卫

### 全局前置守卫

```typescript
router.beforeEach(async (to, from, next) => {
  const authStore = useAuthStore();

  // 设置页面标题
  document.title = to.meta.title
    ? `${to.meta.title} - YuliLog`
    : "YuliLog 工作日志系统";

  // 检查是否需要登录
  if (to.meta.requiresAuth && !authStore.isAuthenticated) {
    return next({ name: "Login", query: { redirect: to.fullPath } });
  }

  // 检查权限
  if (to.meta.permission && !authStore.hasPermission(to.meta.permission)) {
    ElMessage.error("您没有访问该页面的权限");
    return next({ name: "Home" });
  }

  next();
});
```

### 全局后置守卫

```typescript
router.afterEach((to, from) => {
  // 页面切换后的操作
  // 例如：关闭加载状态、记录访问日志等
});
```

## 布局系统

### 主布局结构

```vue
<template>
  <el-container class="main-layout">
    <el-aside width="200px">
      <SidebarNav />
    </el-aside>

    <el-container>
      <el-header>
        <TopNav />
      </el-header>

      <el-main>
        <Breadcrumb v-if="showBreadcrumb" />
        <router-view v-slot="{ Component }">
          <transition name="fade">
            <component :is="Component" />
          </transition>
        </router-view>
      </el-main>

      <el-footer>
        <AppFooter />
      </el-footer>
    </el-container>
  </el-container>
</template>
```

### 侧边栏导航

- 菜单项根据权限动态显示
- 支持多级菜单
- 当前路由高亮显示
- 可折叠/展开
- 当前用户的劳动合同进入到期前 10 天或已经到期时，“入职”菜单显示红色角标，人力资源分组同步显示红点；已离职员工不提示
- 管理员或超级管理员存在试用期截止 30 天内或已到期的待处理员工时，“员工数据”菜单显示红色角标，人力资源分组同步显示红点
- 总经理存在待签署的主管领导意见或普通员工请假待办时，人力资源区“审批中心”显示任务总数；董事长的转正终审、总经理请假、离职及试用期相关待办统一计入“员工数据”角标，管理员和超级管理员的员工数据角标不计入只读抄送记录
- 员工请假审批通过后，主布局在待办轮询中弹出成功通知，“请假”菜单与人力资源分组显示未读角标；员工进入请假页关闭通过提示后清除未读数量

#### 菜单结构（自上而下）

1. **办公区**
   - 日历
   - 今日日志
   - 历史日志

2. **财务区**
   - 基础报销
   - 大额报销
   - 商务报销

3. **人力资源**
   - 入职
   - 转正
   - 请假
   - 审批中心（仅总经理）
   - 员工数据（管理员、超级管理员、董事长）

“转正”页面只展示当前员工本人的申请状态、申请单和历史记录，不承载任何审批任务。总经理从人力资源区“审批中心”签署主管领导意见；管理员和超级管理员从“员工数据 > 转正管理”签署人事部意见；董事长同样从“员工数据 > 转正管理”进入本人负责的转正终审。`/gm-probation-approval` 仅允许总经理访问。
“请假”页面只展示当前员工本人的余额、申请和历史记录，不承载审批任务。总经理从人力资源区“审批中心 > 请假审批”处理普通员工申请；董事长从“员工数据 > 请假管理 > 请假审批”处理总经理申请并查看本人审批记录；管理员和超级管理员在同一管理页只显示“抄送记录”，不显示审批入口或请假待办角标。
离职不再作为员工侧菜单窗口；管理员和超级管理员从“员工数据 > 离职管理”进入。历史 `/resignation` 地址仅对管理员开放，并重定向至 `/employee-data?tab=resignation`。

4. **项目区**
   - 项目管理
   - 项目立项
   - 项目进度
   - 项目封存
   - 预设方案
   - 预设板块
   - 事件库
   - 部门管理

5. **系统区**
   - 系统设置（仅管理员）
   - 用户设置

### 顶部导航栏

- 用户信息展示
- 快捷操作入口
- 退出登录按钮
- 系统通知
- 管理员发起人事档案一键识别后，所有主布局业务页面的上方正中持续显示当前员工、文件、等待数量及识别状态；识别中、完成、需核对和失败分别使用蓝、绿、黄、红背景与边框，任务结束后切换为可手动关闭的持久结果提示，退出登录时清空当前账号的任务提示

## 页面路由列表

| 路径                                       | 页面             | 权限               | 说明                                                                                             |
| ------------------------------------------ | ---------------- | ------------------ | ------------------------------------------------------------------------------------------------ |
| `/`                                        | 首页             | -                  | 重定向到 `/home`                                                                                 |
| `/home`                                    | 工作日志         | `worklogs.view`    | 工作日志编辑页面                                                                                 |
| `/calendar`                                | 日历             | `calendar.view`    | 日程管理页面                                                                                     |
| `/projects`                                | 项目管理         | `projects.view`    | 项目列表页面                                                                                     |
| `/projects/initiation`                     | 项目立项         | `projects.create`  | 项目立项页面                                                                                     |
| `/projects/progress`                       | 项目进度         | `projects.view`    | 项目进度页面                                                                                     |
| `/projects/archive`                        | 项目归档         | `projects.archive` | 项目归档页面                                                                                     |
| `/projects/settlement`                     | 项目结算         | `projects.settle`  | 项目结算页面                                                                                     |
| `/users`                                   | 用户管理         | 管理员、超级管理员、董事长 | 配置 `requiresAdmin`，其他角色禁止进入                                                    |
| `/gm-probation-approval`                   | 人力资源审批中心 | 总经理             | 总经理处理主管领导意见和普通员工请假                                                           |
| `/onboarding/template-preview/:templateId` | 劳动合同预览     | 已登录用户         | 独立于主布局的逐页预览页面，不提供下载或打印入口                                                 |
| `/probation/application`                   | 填写转正申请单   | 已登录用户         | 从转正页进入的独立填写页面，不使用弹窗；非实习期、资料不完整或流程进行中的员工只显示不可申请原因 |
| `/resignation-template-editor/:requestId`  | 离职模板在线编辑 | 管理员、超级管理员 | 从离职人员列表打开独立窗口，五类模板在专用页面切换、保存、下载和一并打印                         |
| `/login`                                   | 登录             | -                  | 登录页面                                                                                         |

## 使用示例

### 编程式导航

```typescript
import { useRouter } from "vue-router";

const router = useRouter();

// 跳转到指定页面
router.push({ name: "Calendar" });

// 带参数跳转
router.push({
  name: "ProjectDetail",
  params: { id: "123" },
});

// 带查询参数跳转
router.push({
  path: "/projects",
  query: { status: "active" },
});

// 返回上一页
router.back();
```

### 路由守卫使用

```typescript
// 在组件内使用路由守卫
import { onBeforeRouteLeave } from "vue-router";

onBeforeRouteLeave((to, from) => {
  if (hasUnsavedChanges.value) {
    const answer = window.confirm("有未保存的更改，确定要离开吗?");
    if (!answer) return false;
  }
});
```

### 动态面包屑

```typescript
// 根据路由生成面包屑
const breadcrumbs = computed(() => {
  return route.matched
    .filter((r) => r.meta.title && r.meta.breadcrumb !== false)
    .map((r) => ({
      title: r.meta.title,
      path: r.path,
    }));
});
```

## 路由懒加载

所有页面组件均使用懒加载，优化首屏加载性能：

```typescript
// 使用动态 import
component: () => import("@/views/Calendar.vue");

// 带注释的分块命名
component: () =>
  import(
    /* webpackChunkName: "calendar" */
    "@/views/Calendar.vue"
  );
```

## 特色功能

### 1. 权限路由

- 根据用户权限动态生成路由
- 无权限路由自动隐藏
- 访问无权限页面自动拦截

### 2. 路由缓存

- 使用 `keep-alive` 缓存页面状态
- 支持自定义缓存策略
- 优化页面切换性能

### 3. 路由过渡

- 页面切换动画效果
- 加载状态提示
- 错误页面友好提示

### 4. 面包屑导航

- 自动生成面包屑
- 支持点击返回上级
- 可配置显示/隐藏

## 注意事项

1. **路由命名**：
   - 所有路由必须设置唯一的 `name`
   - 便于编程式导航和路由跳转

2. **权限控制**：
   - `meta.permission` 与后端权限保持一致
   - 前端权限检查仅用于 UI 展示
   - 后端必须有对应的权限验证
   - `/users` 使用管理员路由守卫；后端 `/api/users` 仍独立校验数据库中的实时角色

3. **懒加载**：
   - 大型页面组件必须使用懒加载
   - 避免首屏加载过大的 bundle

4. **路由嵌套**：
   - 保持路由层级简洁
   - 避免过深的嵌套层级

## 更新日志

- 2026-07-30: 新增董事长角色导航与守卫；董事长继承超级管理员页面权限，并在审批中心只处理转正最终审批，总经理继续处理主管意见和请假
- 2026-07-27: 管理员离职待办角标改为随主布局首次加载；人力资源分组、员工数据菜单和离职管理页签统一统计“档案待完善”与“待确认离职”，不再依赖点击离职页签后才显示
- 2026-07-26: 离职模板在线编辑由员工数据页弹窗调整为独立窗口路由，继续限制管理员和超级管理员访问
- 2026-07-24: 移除员工侧“离职”菜单和离职审批入口，离职统一进入管理员“员工数据 > 离职管理”；历史 `/resignation` 地址改为管理员重定向
- 2026-07-17: 请假菜单新增总经理和超级管理员待审批数量角标，普通管理员不显示不可处理的请假待办；人力资源区分组角标同步计入请假待办
- 2026-07-15: 用户管理页面增加管理员路由守卫，普通用户不能通过前端路由进入；后端接口继续作为最终权限边界
- 2026-06-02: 新增山东十六城文旅宣传海报静态素材
  - 新增 `public/posters/shandong-promo-poster.svg`
  - 海报包含山东主题标题、中心地图视觉、十六城名称与代表文化旅游元素
- 2026-04-10: 隐藏员工数据页面顶部重复标题
  - `/employee-data` 路由标题改为空，主布局顶部栏不再显示“员工数据”
  - `EmployeeData` 页面卡片头部标题移除，保留左侧菜单与页内 Tab 文案不变
- 2026-04-09: 修复员工数据页转正列表时间列显示异常
  - 优化 `EmployeeData` 页面时间格式化逻辑，避免无时区字符串被浏览器错误偏移解析
  - 涉及提交时间与转正时间两列展示
- 2026-04-09: 优化员工数据页转正申请列表对齐与字段文案
  - 转正申请列表末列由“操作”调整为“转正时间”
  - 列表标题与内容统一居中显示，视觉更规整
- 2026-04-09: 优化员工数据页转正管理模块视觉留白
  - 调整 `EmployeeData` 页面转正模板区和转正申请列表区的上下间距
  - 保持现有 Tab 结构、表格内容和操作行为不变
- 2026-04-09: 隐藏员工端转正页面顶部栏标题
  - `MainLayout` 将 `/probation` 的 `pageTitle` 置空
  - 保留页面主体中的“转正申请”标题，仅移除顶部栏重复展示的“转正”文字
- 2026-03-25: 电脑端响应式自适应改造（1280px ~ 2560px+）
  - MainLayout 引入 CSS 变量 `--yl-main-padding-y` / `--yl-main-padding-x` 统一管理内容区 padding
  - 1366px 以下自动缩小内容区 padding（24px 45px → 16px 20px）
  - 所有页面的负 margin 改为引用 CSS 变量，自动跟随 padding 变化
  - 日历页面左栏/右栏在小屏下缩窄（280→240px / 380→320px）
  - 报销页面 upload-right 在小屏下改为垂直布局
  - proof-card / login-box 等固定宽度元素改为 min() 自适应
  - step-container / reimbursement-form 的 max-width 改为 min() 或 100%
  - variables.css 新增 `--yl-breakpoint-notebook: 1366px` 断点
  - utilities.css 的 yl-page-container max-width 从 1920px 调整为 2400px
- 2026-03-13: 实现菜单栏待办事项实时提示功能
  - 财务区和人力资源区分组显示待办小红点
  - 各个菜单项显示具体待办数量角标
  - 基础/大额/商务报销显示待确认收款数量（黄色角标）
  - 转正菜单显示待提交/待审批数量（黄色角标）
  - 审批中心显示待审批数量（红色角标）
  - 每30秒自动轮询刷新，业务操作后立即刷新
  - 折叠状态显示小红点，展开状态显示数字角标
- 2026-03-13: 审批中心付款操作改为弹窗模式，不再跳转到独立付款页面
  - 待办 tab 和未付款 tab 的付款按钮均在当前页面弹窗完成
  - 操作完成后自动刷新列表和统计数据
- 2026-03-12: 新增菜单栏待办实时提示功能
  - 新建 `src/stores/pending.ts` 统一管理待办计数
  - MainLayout 引入 pendingStore，为报销、转正、审批中心菜单项添加角标
  - 路由变化时自动刷新待办计数，业务操作后主动刷新
  - SidebarMenuItem 折叠状态下显示小红点提示
- 2026-03-12: 修复报销详情页（基础/大额/商务）报销月份显示为当前月份而非真实月份的问题，修复 onMounted 异步竞态导致数据不显示的问题
- 2026-03-12: 安全修复 - 清理 MainLayout 和 SidebarGroup 未使用变量/导入，修复 defineModel/defineEmits 宏使用方式
- 2026-01-23: 调整菜单结构 - 将"工作区"改名为"办公区"，移除"行政区"，将入职/转正/离职功能合并到"人力资源"区
- 2026-01-22: 创建文档，描述路由与布局模块核心功能
- 2026-07-17: 入职菜单新增本人劳动合同到期红色角标，沿用员工数据中提前 10 天及过期后持续提醒的规则；移除入职页额外 20px 横向缩进，使页签、提醒和表单板块与其他人力资源页面统一左边界
- 2026-07-17: 人力资源区统一补充请假驳回待办提示；请假菜单角标同时汇总本人待重提数量和本人可审批数量，人力资源区分组同步显示红色提示，修改重提后立即清除
- 2026-07-23: 员工数据菜单新增管理员试用期到期前 30 天提醒角标，人力资源分组同步显示红点
- 2026-07-24: 新增独立劳动合同预览路由，员工从入职文件打开新页面逐页查看，预览页不进入主布局且不提供下载或打印入口
- 2026-07-24: 人力资源审批中心开放给人事管理员处理转正签署，待办角标按主管领导意见、人事部意见和最终审批的实际处理账号统计
- 2026-07-24: 转正申请单改为 `/probation/application` 独立页面；员工从转正页进入填写，提交或取消后返回转正页，不再使用弹窗承载整张申请单
- 2026-07-24: 转正待签路径调整为总经理签署主管领导意见、管理员签署人事部意见和最终审批，审批中心角标按实际阶段统计
- 2026-07-24: 转正审批中心改为三个审批环节均进入管理员待办，系统总经理不再承接主管领导意见签署，角标计数同步按管理员代签规则统计
- 2026-07-24: 更正审批中心待办归属，主管领导意见计入总经理待办，人事部意见和最终总经理审批计入管理员待办；人事部通过后在同一签署窗口继续最终审批
- 2026-07-27: 转正审批入口按角色收口：管理员移除人力资源审批中心并只在“员工数据 > 转正管理”处理，人事待办角标同步迁移；总经理仅从人力资源审批中心处理，个人“转正”页不再显示审批任务
- 2026-07-27: 管理员员工数据待办角标新增“转正审批完成但正式盖章档案未上传”数量，归档上传成功后立即刷新并清除对应提醒
- 2026-07-27: 请假审批入口按角色收口：总经理在人力资源审批中心新增“请假审批”页签，管理员和超级管理员仅在“员工数据 > 请假管理”审批；个人“请假”菜单角标只提示本人驳回待重提，审批待办分别计入审批中心或员工数据角标
- 2026-07-27: 人力资源审批中心改由主布局提供无横向内边距的全宽区域；主内容宽度明确按菜单收起 64 像素、锁定 220 像素同步扣减，状态切换完成后主动触发表格宽度重排，避免右侧内容被裁切
- 2026-07-27: 请假审批仅保留在总经理的人力资源审批中心；管理员和超级管理员的员工数据页移除请假审批页签及待办角标，请假管理默认进入“抄送记录”
- 2026-07-28: 新增羽隶经营看板路由和专属导航，BOSS登录后进入只读看板并隐藏普通业务区与待办入口。
- 2026-07-28: BOSS账号豁免入职资料检查，登录及手动访问入职页面时均直接进入经营看板；顶部和侧栏账号菜单在未配置邮箱时不显示空白邮箱行。
- 2026-07-28: 导航、账号菜单和经营看板标题中的角色显示名称统一调整为 BOSS。
- 2026-07-28: 羽隶经营看板升级为深色驾驶舱首屏与悬浮趋势控制区，增加全屏查看、响应式指标栅格和月度分析侧栏。
- 2026-07-28: 页面标题、侧栏导航和路由名称统一调整为“羽隶经营看板”，内部BOSS角色编码保持不变。
- 2026-07-29: 羽隶经营看板改为浅色紧凑首屏，并拆分为经营概览、财务分析、人员与日志、项目与风险四个互斥分区；财务周期支持精确自然月或自然年，当前实时快照和 30 秒刷新规则保持明确。
- 2026-07-30: 请假审批通过新增员工全局成功通知、请假菜单未读角标和人力资源分组提示，员工确认查看后清除
- 2026-07-30: 董事长移除人力资源区独立审批中心入口，转正终审统一迁移到“员工数据 > 转正管理”；独立审批中心路由仅保留总经理访问
- 2026-07-31: 总经理请假待办和审批记录迁入董事长的“员工数据 > 请假管理 > 请假审批”，对应角标归入员工数据；独立人力资源审批中心继续仅向总经理开放，管理员和超级管理员仍只查看抄送记录
- 2026-08-04: 主布局页面上方正中新增管理员人事档案全局识别状态，关闭员工窗口或切换业务页面后继续以蓝、绿、黄、红状态色显示等待、识别中和最终结果，完成提示保留至手动关闭，退出登录时统一清理
