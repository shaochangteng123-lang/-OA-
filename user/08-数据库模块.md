# 08 - 数据库模块

## 模块概述

数据库模块负责 PostgreSQL 数据库的初始化、表结构定义、数据迁移和种子数据管理。

## 主要功能

### 1. 数据库初始化

- PostgreSQL 数据库连接
- 表结构自动创建
- 索引和约束设置
- 默认数据填充

### 2. 数据表管理

- 用户表
- 项目表
- 工作日志表
- 日历事件表
- 事件库表
- 节假日表
- 会话表

### 3. 数据迁移

- 数据库版本管理
- 结构变更迁移
- 数据迁移脚本

### 4. 种子数据

- 初始化数据填充
- 测试数据生成
- 开发环境数据

## 核心文件

- [server/db/index.ts](../server/db/index.ts) - 数据库初始化和表结构定义
- [server/db/seed.ts](../server/db/seed.ts) - 种子数据（如存在）
- [server/scripts/update-holidays.ts](../server/scripts/update-holidays.ts) - 节假日数据更新脚本

## 数据库配置

### 连接配置

```typescript
import { Pool } from "pg";

const connectionString =
  process.env.DATABASE_URL || "postgresql://localhost:5432/yulilog_worklog";
const pool = new Pool({ connectionString });

// PostgreSQL 默认启用外键约束，无需额外配置
```

## 表结构

> 说明：当前生产环境以 `server/db/index.ts` 中的 PostgreSQL 建表语句为准。  
> 本节部分历史示例保留了旧字段命名，仅用于理解业务关系，不作为迁移执行脚本。

### 1. Users（用户表）

```sql
CREATE TABLE IF NOT EXISTS users (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  email TEXT,
  feishu_user_id TEXT UNIQUE,
  feishu_username TEXT,
  avatar_url TEXT,
  role TEXT DEFAULT 'user',
  is_active BOOLEAN DEFAULT 1,
  last_login_at DATETIME,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_users_feishu_user_id ON users(feishu_user_id);
CREATE INDEX idx_users_role ON users(role);
```

#### 员工编号一致性

- `users.email`、`users.mobile`、`users.department`、`users.position` 和 `users.employee_no` 均允许为空，以支持不属于员工体系的BOSS和董事长轻量账号
- `users.employee_no` 是员工账号编号的唯一数据来源，创建员工账号时由后端自动生成，编辑时可由管理员修改
- 数据库通过条件唯一索引 `uq_users_employee_no` 阻止非空员工编号重复
- 创建员工账号时后端在同一事务锁内扫描已有有效编号并写入账号与员工档案；创建BOSS或董事长账号时保持为空且不创建 `employee_profiles`
- 用户接口修改编号时，在同一事务内同步 `employee_profiles.employee_no`
- 用户接口修改编号时不更新 `employee_documents.file_name`，已归档资料的文件名和文件内容保持管理员上传时的状态
- 数据库启动时不再为空编号账号自动补号；新账号编号只在用户创建事务中生成
- 数据库启动迁移只用账号中的非空编号修复 `employee_profiles` 基础档案记录，不会改写 `employee_documents` 人事档案文件；入职模板下载优先读取账号中的最新值

```sql
CREATE UNIQUE INDEX IF NOT EXISTS uq_users_employee_no
ON users(employee_no)
WHERE employee_no IS NOT NULL AND BTRIM(employee_no) <> '';
```

#### 个人电子签名表 (`user_signatures`)

```sql
CREATE TABLE IF NOT EXISTS user_signatures (
  user_id TEXT PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
  signature_path TEXT NOT NULL,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL
);
```

- 每个账号只能首次确认一份个人电子签名；记录存在后上传和删除接口均拒绝修改
- 表内只保存受鉴权保护的图片路径，不保存图片数据或公开地址
- 转正流程签署时会把当前个人签名复制到 `probation_signature_records`，历史签名不依赖本表当前内容

#### 管理员代管签名表 (`user_delegated_signatures`)

```sql
CREATE TABLE IF NOT EXISTS user_delegated_signatures (
  user_id TEXT PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
  represented_user_id TEXT REFERENCES users(id) ON DELETE SET NULL,
  represented_user_name TEXT NOT NULL,
  signature_path TEXT NOT NULL,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL
);
```

- 该表只为兼容历史代签审计数据保留；当前接口和页面不再新增、读取或维护代管签名
- 新转正流程中的员工、总经理、管理员和董事长均只调用各自 `user_signatures` 记录

### 2. Projects（项目表）

```sql
CREATE TABLE IF NOT EXISTS projects (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  project_code TEXT UNIQUE,
  type TEXT,
  status TEXT DEFAULT 'PLANNING',
  district TEXT,
  description TEXT,
  start_date DATE,
  end_date DATE,
  contact_person TEXT,
  contact_phone TEXT,
  budget REAL,
  actual_cost REAL,
  created_by INTEGER NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (created_by) REFERENCES users(id)
);

CREATE INDEX idx_projects_status ON projects(status);
CREATE INDEX idx_projects_district ON projects(district);
CREATE INDEX idx_projects_created_by ON projects(created_by);
```

### 3. Worklogs（工作日志表）

```sql
CREATE TABLE IF NOT EXISTS worklogs (
  id TEXT PRIMARY KEY,
  user_id INTEGER NOT NULL,
  log_date DATE NOT NULL,
  content TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id),
  UNIQUE(user_id, log_date)
);

CREATE INDEX idx_worklogs_user_date ON worklogs(user_id, log_date);
```

### 4. CalendarEvents（日历事件表）

```sql
CREATE TABLE IF NOT EXISTS calendar_events (
  id TEXT PRIMARY KEY,
  title TEXT NOT NULL,
  description TEXT,
  start_time DATETIME NOT NULL,
  end_time DATETIME NOT NULL,
  all_day BOOLEAN DEFAULT 0,
  color TEXT,
  recurrence_rule TEXT,
  created_by INTEGER NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (created_by) REFERENCES users(id)
);

CREATE INDEX idx_calendar_events_time ON calendar_events(start_time, end_time);
CREATE INDEX idx_calendar_events_created_by ON calendar_events(created_by);
```

### 5. EventLibrary（事件库表）

```sql
CREATE TABLE IF NOT EXISTS event_library (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  description TEXT,
  default_duration INTEGER,
  color TEXT,
  category TEXT,
  government_dept_id INTEGER,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_event_library_category ON event_library(category);
```

### 6. Holidays（节假日表）

```sql
CREATE TABLE IF NOT EXISTS holidays (
  id TEXT PRIMARY KEY,
  date DATE NOT NULL UNIQUE,
  name TEXT NOT NULL,
  is_workday BOOLEAN DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_holidays_date ON holidays(date);
```

### 7. LeaveTypeConfigs（假期类型配置表）

```sql
CREATE TABLE IF NOT EXISTS leave_type_configs (
  id TEXT PRIMARY KEY,
  code TEXT UNIQUE NOT NULL,
  name TEXT NOT NULL,
  requires_attachment BOOLEAN DEFAULT FALSE,
  requires_balance_check BOOLEAN DEFAULT TRUE,
  default_days NUMERIC(5,1),
  description TEXT,
  sort_order INTEGER DEFAULT 0,
  is_active BOOLEAN DEFAULT TRUE,
  created_at TEXT NOT NULL
);
```

### 8. LeaveBalances（假期余额表）

```sql
CREATE TABLE IF NOT EXISTS leave_balances (
  id TEXT PRIMARY KEY,
  user_id TEXT NOT NULL REFERENCES users(id),
  leave_type_code TEXT NOT NULL REFERENCES leave_type_configs(code),
  year INTEGER NOT NULL,
  total_days NUMERIC(5,1) NOT NULL,
  used_days NUMERIC(5,1) NOT NULL DEFAULT 0,
  pending_days NUMERIC(5,1) NOT NULL DEFAULT 0,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL,
  UNIQUE(user_id, leave_type_code, year)
);
```

**假期余额同步口径：**

- 年假以 `leave_type_configs.default_days` 作为所有活动账号的基础额度；有有效入职日期时再按工龄上调，旧版年假说明在启动时幂等更新
- 假期类型默认天数变更时，仅同步当年 `total_days` 仍等于旧默认天数的余额记录
- 管理员手动调整过的个人余额不会被假期类型默认天数覆盖
- 年假个人总额低于基础额度时会在读取时自动补足；补足只更新 `total_days`，不清零 `used_days` 或 `pending_days`
- 调整个人余额时必须满足 `total_days >= used_days + pending_days`，检查和更新在同一事务内加行锁完成
- 启动迁移会识别“999 天且不校验余额”的旧版事假哨兵配置，改为每年 3 天带薪事假，并同步未发生使用或预占冲突的 999 天历史余额

### 8.1 LeaveRequests（请假申请表）

```sql
CREATE TABLE IF NOT EXISTS leave_requests (
  id TEXT PRIMARY KEY,
  request_no TEXT UNIQUE NOT NULL,
  user_id TEXT NOT NULL REFERENCES users(id),
  leave_type_code TEXT NOT NULL REFERENCES leave_type_configs(code),
  start_date TEXT NOT NULL,
  start_half TEXT NOT NULL,
  end_date TEXT NOT NULL,
  end_half TEXT NOT NULL,
  total_days NUMERIC(5,1) NOT NULL,
  balance_allocations_json TEXT,
  balance_reserved BOOLEAN NOT NULL DEFAULT FALSE,
  approval_notice_unread BOOLEAN NOT NULL DEFAULT FALSE,
  rejection_notice_unread BOOLEAN NOT NULL DEFAULT FALSE,
  application_kind TEXT NOT NULL DEFAULT 'normal'
    CHECK(application_kind IN ('normal', 'combined', 'extension', 'supplement')),
  combination_group_id TEXT,
  parent_request_id TEXT REFERENCES leave_requests(id) ON DELETE SET NULL,
  status TEXT NOT NULL DEFAULT 'pending'
    CHECK(status IN ('draft', 'pending', 'approved', 'rejected', 'cancelled'))
);
```

**余额快照说明：**

- `request_no` 使用 `QJ-YYYY-NNNNN` 格式并保持唯一；启动迁移将历史 `LR` 前缀转换为 `QJ`，遇到目标编号冲突时按对应年度最大序号继续编号
- `balance_allocations_json` 保存申请提交时按自然年拆分的天数，跨年申请审批时分别结算对应年份余额
- `balance_reserved` 保存提交时该假期类型是否需要余额检查，避免管理员后续修改类型配置后造成预占天数无法释放或错误扣减
- `approval_notice_unread` 保存审批通过结果是否仍待员工查看；历史记录迁移时默认已读，新的审批通过记录置为未读，员工确认后恢复为 `FALSE`
- `rejection_notice_unread` 保存审批驳回结果是否仍待员工查看；现有最新驳回记录迁移为未读，员工打开对应申请详情后按条恢复为 `FALSE`
- `application_kind` 区分普通请假、组合分段、续假和补假；驳回重提不会改变该业务类型
- `combination_group_id` 关联一次组合提交拆出的多个独立申请；普通组合分段使用 `combined`，组合续假或组合补假分段继续使用 `extension` 或 `supplement`，各记录仍分别保存申请编号、审批状态和余额快照
- `parent_request_id` 将单一及组合续假、补假统一挂到已批准主申请下，并与仅用于驳回重提版本链的 `original_id` 分离；删除主记录时关联值自动置空
- `idx_leave_requests_approval_notice` 和 `idx_leave_requests_rejection_notice` 分别加速审批通过、驳回的本人未读提醒统计
- `idx_leave_requests_combination_group` 和 `idx_leave_requests_parent_request` 分别加速组合详情和主子申请查询
- 新提交和重新提交通过事务行锁预占 `pending_days`，审批、驳回、撤回再按快照转为已使用或释放
- 婚假一次性资格通过 `employee_profiles.id_number` 关联历史申请；提交事务按规范化身份证号获取事务级锁，并检查同一身份证号是否已有审批中或已批准婚假，不新增年度重复资格字段
- 审批中申请撤回时更新为 `draft`，同时将 `balance_reserved` 设为 `FALSE`；草稿重新提交后重新预占余额并恢复为 `pending`
- `cancelled` 仅保留在检查约束中兼容旧备份；数据库启动时会将现有 `cancelled` 记录迁移为 `draft` 并清除余额占用标记，新撤回不再写入该状态
- 草稿手动删除和跨月自动清理均采用硬删除：先删除整条版本链的 `leave_approval_logs` 与 `leave_attachments`，再按版本倒序删除 `leave_requests`，事务提交后清理磁盘附件

**审批人数据规则：**

- `approver_id` 和 `approver_name` 保存提交时选定的具体审批人；普通员工保存活动总经理，总经理保存活动董事长，申请人本人不会被选为审批人
- 查询请假流程时，总经理和董事长的显示身份直接取账号角色名称，不使用员工档案职位覆盖审批角色
- 对应角色没有可用审批人时不创建请假申请，不回退到管理员或超级管理员
- 启动迁移会逐条重算历史待审批申请：普通员工待办分配给活动总经理，总经理待办分配给活动董事长；暂时没有对应审批人时清空旧的错误分配
- 管理员抄送采用角色范围读取，不重复写入抄送明细；管理员和超级管理员可读取全员申请，但不能更新审批状态

### 8.2 人力成本工资表

`employee_documents` 为劳动合同档案增加以下期限字段：

```sql
contract_start_date TEXT,
contract_end_date TEXT,
contract_recognized_at TEXT,
probation_end_date TEXT
```

`employee_profiles` 另行保存待签劳动合同模板的拟定日期：

```sql
contract_template_start_date TEXT,
contract_template_end_date TEXT,
probation_template_start_date TEXT,
probation_template_end_date TEXT
```

归档后还会保存上一期模板预览快照：

```sql
last_contract_template_start_date TEXT,
last_contract_template_end_date TEXT,
last_probation_template_start_date TEXT,
last_probation_template_end_date TEXT,
last_contract_template_position TEXT
```

- 模板日期允许为空，续签合同可只填写合同期限并将试用期保持为空
- 正式合同归档时清空当前四个模板日期字段以锁定下载；历史上一期日期和 `last_contract_template_position` 字段仅保留兼容，不再用于最新版模板预览，未设置当前期限时模板日期保持空白且岗位实时读取员工当前职位
- 启动迁移会使用最近一份已识别正式合同回填缺失的上一期快照，兼容已经被清空过模板日期的员工
- 模板日期不参与正式入职、合同到期和转正数据计算；正式日期继续取已归档劳动合同的识别结果
- 同一员工可保存多条 `document_type='contract'` 的档案记录，构成续签历史
- `employee_documents.document_type='other'` 保存员工提供但不属于固定十类的其他资料；该类型允许同一员工保存多份文件
- `employee_profiles.hire_date` 是系统维护的入职日期快照，来源为该员工第一份已识别完整起止日期合同的 `contract_start_date`；没有可识别合同时必须为空
- `employee_profiles.contract_end_date` 是当前合同结束日期的查询快照，来源为合同档案中最大的 `contract_end_date`
- `employee_documents.probation_end_date` 保存每份劳动合同 OCR（文字识别）出的试用期截止日期；员工转正使用第一份合同的值，续签合同为空不会覆盖
- 不再根据 `hire_date` 推算合同到期时间；启动迁移会清理没有合同依据且恰好等于“入职日期加一年”的旧推算值
- 启动迁移会回填已有识别合同员工的入职日期、当前合同到期日期，以及待提交或已驳回转正记录的合同日期；没有识别合同的历史员工数据不做推算

```sql
CREATE TABLE IF NOT EXISTS employee_salary_profiles (
  employee_id TEXT PRIMARY KEY REFERENCES employee_profiles(id) ON DELETE CASCADE,
  initial_monthly_salary NUMERIC NOT NULL CHECK(initial_monthly_salary >= 0),
  source_document_id TEXT REFERENCES employee_documents(id) ON DELETE SET NULL,
  recognized_at TEXT NOT NULL,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS payroll_records (
  id TEXT PRIMARY KEY,
  employee_id TEXT NOT NULL REFERENCES employee_profiles(id) ON DELETE CASCADE,
  payroll_month TEXT NOT NULL,
  automatic_salary NUMERIC NOT NULL DEFAULT 0 CHECK(automatic_salary >= 0),
  monthly_salary NUMERIC NOT NULL DEFAULT 0 CHECK(monthly_salary >= 0),
  housing_fund_base NUMERIC NOT NULL DEFAULT 0 CHECK(housing_fund_base >= 0),
  contribution_base NUMERIC NOT NULL DEFAULT 0 CHECK(contribution_base >= 0),
  individual_income_tax NUMERIC NOT NULL DEFAULT 0 CHECK(individual_income_tax >= 0),
  monthly_salary_is_manual BOOLEAN NOT NULL DEFAULT FALSE,
  housing_fund_base_is_manual BOOLEAN NOT NULL DEFAULT FALSE,
  contribution_base_is_manual BOOLEAN NOT NULL DEFAULT FALSE,
  tax_is_manual BOOLEAN NOT NULL DEFAULT FALSE,
  version INTEGER NOT NULL DEFAULT 1 CHECK(version >= 1),
  updated_by TEXT REFERENCES users(id),
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL,
  UNIQUE(employee_id, payroll_month)
);

CREATE TABLE IF NOT EXISTS payroll_change_logs (
  id TEXT PRIMARY KEY,
  payroll_record_id TEXT NOT NULL REFERENCES payroll_records(id) ON DELETE CASCADE,
  employee_id TEXT NOT NULL REFERENCES employee_profiles(id) ON DELETE CASCADE,
  payroll_month TEXT NOT NULL,
  field_name TEXT NOT NULL CHECK(field_name IN ('monthly_salary', 'housing_fund_base', 'contribution_base', 'individual_income_tax')),
  old_value NUMERIC NOT NULL CHECK(old_value >= 0),
  new_value NUMERIC NOT NULL CHECK(new_value >= 0),
  changed_by TEXT NOT NULL REFERENCES users(id),
  changed_at TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS human_cost_receipts (
  id TEXT PRIMARY KEY,
  payroll_month TEXT NOT NULL,
  category TEXT NOT NULL CHECK(category IN ('social_security', 'housing_fund', 'income_tax', 'net_salary')),
  file_name TEXT NOT NULL,
  file_path TEXT NOT NULL,
  file_size INTEGER NOT NULL CHECK(file_size > 0),
  mime_type TEXT NOT NULL CHECK(mime_type IN ('application/pdf', 'image/jpeg', 'image/png')),
  file_hash TEXT NOT NULL,
  recognized_amount NUMERIC(14,2) NOT NULL DEFAULT 0 CHECK(recognized_amount >= 0),
  recognition_status TEXT NOT NULL DEFAULT 'processing'
    CHECK(recognition_status IN ('processing', 'recognized', 'partial', 'failed')),
  recognized_item_count INTEGER NOT NULL DEFAULT 0 CHECK(recognized_item_count >= 0),
  total_item_count INTEGER NOT NULL DEFAULT 0 CHECK(total_item_count >= 0),
  ignored_item_count INTEGER NOT NULL DEFAULT 0 CHECK(ignored_item_count >= 0),
  recognition_version INTEGER NOT NULL DEFAULT 1 CHECK(recognition_version >= 1),
  recognition_error TEXT,
  uploaded_by TEXT NOT NULL REFERENCES users(id),
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL,
  UNIQUE(payroll_month, category, file_hash)
);

CREATE TABLE IF NOT EXISTS payroll_tax_detail_files (
  id TEXT PRIMARY KEY,
  payroll_month TEXT NOT NULL UNIQUE,
  file_name TEXT NOT NULL,
  file_path TEXT NOT NULL,
  file_size INTEGER NOT NULL,
  mime_type TEXT NOT NULL,
  recognized_count INTEGER NOT NULL,
  zero_count INTEGER NOT NULL,
  missing_employee_names JSONB NOT NULL DEFAULT '[]'::jsonb,
  unreadable_employee_names JSONB NOT NULL DEFAULT '[]'::jsonb,
  page_count INTEGER NOT NULL,
  uploaded_by TEXT NOT NULL REFERENCES users(id),
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS human_cost_receipt_items (
  id TEXT PRIMARY KEY,
  receipt_id TEXT NOT NULL REFERENCES human_cost_receipts(id) ON DELETE CASCADE,
  employee_id TEXT REFERENCES employee_profiles(id) ON DELETE SET NULL,
  payee_name TEXT NOT NULL,
  payee_account TEXT NOT NULL,
  amount NUMERIC(14,2) NOT NULL CHECK(amount > 0),
  proof_no TEXT,
  page_no INTEGER NOT NULL CHECK(page_no >= 1),
  position TEXT NOT NULL CHECK(position IN ('full', 'top', 'bottom')),
  match_status TEXT NOT NULL CHECK(match_status IN ('matched', 'unmatched', 'ambiguous')),
  created_at TEXT NOT NULL,
  UNIQUE(receipt_id, page_no, position)
);
```

- 工资基础金额使用无固定小数位的 `NUMERIC`（精确数值）；公式内部按精确十进制计算，返回表格时统一四舍五入为两位小数
- `housing_fund_base` 保存公积金缴费基数，`contribution_base` 保存社保缴费基数；单位和个人公积金均使用前者，单位和个人社保均使用后者
- 单位与个人承担明细不重复存储，由统一计算服务根据月度基础字段实时推导，避免公式修改后出现字段不一致
- 四个手工标记区分自动值和管理员改单值；普通自动重算不覆盖手工值，新上传正式邀请函时仅重置本月工资手工标记，公积金基数、社保基数和个税手工标记不变
- 历史月份保留金额快照；主动生成月份时同步人员成员关系，入职当月加入、离职当月保留、离职次月移除
- 一键删除人事档案时只删除该员工的 `employee_documents`，同时清空 `employee_profiles.contract_end_date`；`employee_salary_profiles` 与 `payroll_records` 保留，来源文件外键通过 `ON DELETE SET NULL` 自动置空
- 批量档案删除与合同日期清空在同一事务中完成；删除过劳动合同时，当前及未来自动工资按无合同档案状态重新同步，历史月份和管理员手工金额保持不变
- 数据库初始化会清理关联用户角色为 `super_admin` 的工资记录，系统维护账号不纳入人力成本数据
- 工资查询、生成、修改和自动同步均排除 `boss`（BOSS）与 `chairman`（董事长）；不删除既有历史记录，但当前角色为轻量账号时不计入人员和人力成本口径
- `payroll_records.version` 为多人修改提供乐观锁；自动工资变化和管理员修改都会递增版本
- `payroll_change_logs` 保存管理员修改前后金额、操作人和时间，随工资记录删除而级联清理
- 个人个税明细识别复用 `payroll_records.individual_income_tax` 和 `tax_is_manual`，每个成功匹配员工递增记录版本；金额实际变化时写入 `payroll_change_logs`
- `payroll_tax_detail_files` 每个工资月份保存一份最新的个人个税明细原件、识别人数、0 元人数及未识别名单；重新上传同月明细时事务替换记录并清理旧原件，支持页面刷新后继续在线预览
- `human_cost_receipts` 保存公司级社保、公积金、个税和实发工资汇总回单；月份为工资归属月份，不是文件上传日期
- `human_cost_receipt_items` 仅保存通过校验的实发工资单笔结果，按收款账号或收款户名精确关联员工，并保存原 PDF（便携式文档格式）的页码及上下位置；来源回单删除时单笔记录级联删除
- 回单识别金额固定保存两位小数；`recognized_item_count`、`total_item_count` 与 `ignored_item_count` 分别记录成功、校验范围及非工资回单忽略笔数
- `recognition_version` 标识识别规则版本；旧版回单在管理员再次打开对应月份时自动进入后台重识别，无需删除后重新上传
- 文件摘要唯一约束只覆盖同一工资月份和同一费用类别，既防止重复汇总，也允许同一原件在确有需要时归入其他月份或类别
- 回单索引覆盖工资月份与类别、识别状态，支持月份切换查询和处理状态轮询
- 用户角色、账号状态、员工状态、档案类型、转正状态、请假状态、半天时段、余额和工资版本均有数据库检查约束；各类历史数据约束独立迁移，单类旧脏数据不会回滚工资版本字段
- 用户角色检查约束支持 `boss`（BOSS）和 `chairman`（董事长）；启动迁移同步更新既有数据库约束，董事长角色与内部兼容保留的最终审批阶段编码保持独立。

### 9. Drafts（草稿表）

```sql
CREATE TABLE IF NOT EXISTS drafts (
  id TEXT PRIMARY KEY,
  user_id INTEGER NOT NULL,
  draft_date DATE NOT NULL,
  content TEXT,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id),
  UNIQUE(user_id, draft_date)
);

CREATE INDEX idx_drafts_user_date ON drafts(user_id, draft_date);
```

### 8. Sessions（会话表）

```sql
CREATE TABLE IF NOT EXISTS sessions (
  sid TEXT PRIMARY KEY,
  sess TEXT NOT NULL,
  expired INTEGER NOT NULL
);

CREATE INDEX idx_sessions_expired ON sessions(expired);
```

### 9. Human Resources（人事流程表）

```sql
CREATE TABLE IF NOT EXISTS probation_confirmations (
  id TEXT PRIMARY KEY,
  employee_id TEXT NOT NULL REFERENCES employee_profiles(id) ON DELETE CASCADE,
  hire_date TEXT,
  probation_end_date TEXT,
  status TEXT DEFAULT 'pending'
    CHECK(status IN ('pending', 'submitted', 'approved', 'rejected')),
  form_version INTEGER NOT NULL DEFAULT 0,
  review_stage TEXT NOT NULL DEFAULT 'employee'
    CHECK(review_stage IN ('employee', 'supervisor', 'hr', 'general_manager', 'completed')),
  conversion_type TEXT NOT NULL DEFAULT 'normal'
    CHECK(conversion_type IN ('normal', 'early', 'extended', 'other')),
  conversion_type_other TEXT,
  self_statement TEXT,
  applicant_name_snapshot TEXT,
  department_snapshot TEXT,
  position_snapshot TEXT,
  supervisor_id TEXT REFERENCES users(id),
  supervisor_name_snapshot TEXT,
  hr_approver_id TEXT REFERENCES users(id),
  hr_approver_name_snapshot TEXT,
  chairman_id TEXT REFERENCES users(id),
  chairman_name_snapshot TEXT,
  submit_time TEXT,
  approve_time TEXT,
  approver_id TEXT REFERENCES users(id),
  approver_comment TEXT,
  application_comment TEXT,
  formal_document_generated_at TEXT,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL
);

CREATE UNIQUE INDEX IF NOT EXISTS uq_probation_confirmations_employee_id
ON probation_confirmations(employee_id);

CREATE UNIQUE INDEX IF NOT EXISTS uq_approval_instances_pending_probation_target
ON approval_instances(target_id)
WHERE target_type = 'probation' AND status = 'pending';

CREATE TABLE IF NOT EXISTS probation_documents (
  id TEXT PRIMARY KEY,
  confirmation_id TEXT REFERENCES probation_confirmations(id) ON DELETE CASCADE,
  employee_id TEXT REFERENCES employee_profiles(id) ON DELETE CASCADE,
  document_type TEXT NOT NULL,
  file_name TEXT NOT NULL,
  file_path TEXT NOT NULL,
  file_size INTEGER,
  mime_type TEXT,
  uploaded_by TEXT NOT NULL REFERENCES users(id),
  uploaded_by_name TEXT,
  source_type TEXT NOT NULL DEFAULT 'uploaded'
    CHECK(source_type IN ('uploaded', 'generated', 'official')),
  form_version INTEGER NOT NULL DEFAULT 0,
  created_at TEXT NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_probation_documents_employee_id
ON probation_documents(employee_id);

CREATE TABLE IF NOT EXISTS probation_signature_records (
  id TEXT PRIMARY KEY,
  confirmation_id TEXT NOT NULL
    REFERENCES probation_confirmations(id) ON DELETE CASCADE,
  form_version INTEGER NOT NULL,
  stage TEXT NOT NULL
    CHECK(stage IN ('employee', 'supervisor', 'hr', 'general_manager')),
  signer_id TEXT NOT NULL REFERENCES users(id),
  signer_name TEXT NOT NULL,
  signer_role TEXT NOT NULL,
  signer_department TEXT,
  signer_position TEXT,
  signature_path TEXT NOT NULL,
  signature_type TEXT NOT NULL DEFAULT 'personal'
    CHECK(signature_type IN ('personal', 'general_manager')),
  signature_owner_name TEXT NOT NULL,
  opinion TEXT,
  decision TEXT NOT NULL CHECK(decision IN ('submit', 'approve', 'reject')),
  signed_at TEXT NOT NULL,
  created_at TEXT NOT NULL,
  UNIQUE(confirmation_id, form_version, stage)
);

CREATE TABLE IF NOT EXISTS probation_templates (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  file_name TEXT NOT NULL,
  file_path TEXT NOT NULL,
  file_size INTEGER,
  mime_type TEXT,
  uploaded_by TEXT NOT NULL REFERENCES users(id),
  uploaded_by_name TEXT,
  created_at TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS resignation_requests (
  id TEXT PRIMARY KEY,
  employee_id TEXT NOT NULL REFERENCES employee_profiles(id) ON DELETE CASCADE,
  employee_user_id TEXT NOT NULL REFERENCES users(id),
  handover_user_id TEXT NOT NULL REFERENCES users(id),
  handover_name TEXT,
  resign_type TEXT NOT NULL,
  resign_date TEXT NOT NULL,
  reason TEXT,
  status TEXT NOT NULL DEFAULT 'draft',
  employee_confirm_time TEXT,
  handover_confirm_time TEXT,
  submit_time TEXT,
  approve_time TEXT,
  approver_id TEXT REFERENCES users(id),
  approver_comment TEXT,
  reject_target TEXT,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL,
  UNIQUE(employee_id)
);

CREATE TABLE IF NOT EXISTS resignation_documents (
  id TEXT PRIMARY KEY,
  request_id TEXT NOT NULL REFERENCES resignation_requests(id) ON DELETE CASCADE,
  document_type TEXT NOT NULL,
  uploader_role TEXT NOT NULL,
  file_name TEXT NOT NULL,
  file_path TEXT NOT NULL,
  file_size INTEGER,
  mime_type TEXT,
  uploaded_by TEXT NOT NULL REFERENCES users(id),
  uploaded_by_name TEXT,
  created_at TEXT NOT NULL,
  is_current INTEGER NOT NULL DEFAULT 1
);

CREATE TABLE IF NOT EXISTS resignation_templates (
  id TEXT PRIMARY KEY,
  template_type TEXT NOT NULL,
  name TEXT NOT NULL,
  file_name TEXT NOT NULL,
  file_path TEXT NOT NULL,
  file_size INTEGER,
  mime_type TEXT,
  uploaded_by TEXT NOT NULL REFERENCES users(id),
  uploaded_by_name TEXT,
  created_at TEXT NOT NULL
);
```

**说明：**

- `probation_confirmations.employee_id` 使用唯一索引，确保每名员工只有一条当前转正记录；新账号不预建记录，劳动合同识别出入职日期和试用期截止后再创建
- `probation_confirmations.hire_date` 允许为空；无可识别劳动合同时与员工档案同步清空
- `probation_confirmations.probation_end_date` 允许为空；第一份劳动合同未设置试用期时保持空值，不能沿用续签合同或历史推算日期
- `probation_confirmations.form_version` 标识员工每次重新提交的在线申请版本，`review_stage` 标识员工、主管、人事部、董事长终审或已完成阶段；数据库继续使用 `general_manager` 值兼容既有记录
- 申请人、部门和职位使用提交时快照生成待盖章底稿，员工后续调岗不会改写已经签署版本
- `probation_confirmations` 在每次提交时固定保存主管、管理员和董事长的账号及姓名快照；当前人员后续改名、停用或换人不会改写该版流程，重新提交的新版本才重新分配
- `probation_confirmations.supervisor_id` 固定为本次签署主管领导意见的总经理账号，`formal_document_generated_at` 仅在董事长完成最终审批并成功生成待盖章底稿后写入
- 待处理的转正审批实例使用条件唯一索引，确保同一转正记录同时最多存在一个待审批实例
- `probation_documents.employee_id` 标识文件所属员工；现有流程文件会从转正记录自动回填员工编号
- `probation_documents.confirmation_id` 可为空，支持直接在职员工补录转正申请表且不生成虚假审批记录；每份文件必须至少关联转正记录或员工档案
- `probation_documents.source_type=generated` 标识系统终审生成的待盖章底稿；`source_type=official` 标识管理员在人事转正档案上传的正式盖章文件；`uploaded` 仅保留历史上传材料，两者均用 `form_version` 绑定申请版本
- `probation_signature_records` 保存四个阶段的签名图像路径、实际操作人、签名类型、签名人姓名、意见、决定和服务端签署时间；同一申请版本的同一阶段只能有一条记录，驳回重提不会覆盖旧版本
- 历史记录中的 `signature_type=general_manager` 仅用于兼容旧代签数据；当前新记录统一使用签署人锁定后的本人签名，`signer_id`、`signer_name` 和 `signature_owner_name` 均记录实际签署人
- `resignation_requests` 保存管理员创建的离职人员记录；新记录使用 `draft`（档案待完善），五类档案齐全后仍保持 `draft` 供管理员预览核对，管理员确认离职时才转为 `approved`（已完成）。旧流程字段继续兼容历史数据，但新流程不再使用员工申请、交接或终审。
- `resignation_requests.employee_id` 采用唯一约束，确保同一员工档案只存在一条离职记录；返聘通过新的账号和员工档案形成新的生命周期。
- `resignation_documents` 固定支持终止 / 解除劳动关系协议书、员工离职交接单、薪资及各类款项结算确认书、离职经济补偿协议书、离职证明；`is_current` 标识当前版本，替换时保留历史版本。
- `resignation_templates` 的新增记录只保存五类管理员 PDF（便携式文档格式）原模板；历史 DOCX（开放 XML 文字处理文档）记录保留兼容，每次上传新模板后读取同类型最新版本。
- `resignation_template_drafts.overlay_json` 保存具体离职记录、模板类型和模板版本对应的字段覆盖值及自定义文字位置；`template_id` 防止模板更新后沿用旧模板草稿，旧 `content_html` 仅保留兼容。
- 管理员确认五类当前档案无误时，在同一数据库事务中更新 `resignation_requests.status`、`employee_profiles.employment_status`、`users.status` 和审计日志；单纯上传齐全不再修改员工或账号状态。

## 数据库操作

### 基础查询

```typescript
import { db } from "./db";

// 查询单条
const user = db.prepare("SELECT * FROM users WHERE id = ?").get(userId);

// 查询多条
const users = db.prepare("SELECT * FROM users WHERE role = ?").all("admin");

// 插入
const result = db
  .prepare(
    `
  INSERT INTO users (name, email, role)
  VALUES (?, ?, ?)
`,
  )
  .run(name, email, role);

const newUserId = result.lastInsertRowid;

// 更新
db.prepare("UPDATE users SET role = ? WHERE id = ?").run(role, userId);

// 删除
db.prepare("DELETE FROM users WHERE id = ?").run(userId);
```

### 事务处理

```typescript
const createProject = db.transaction((projectData) => {
  // 插入项目
  const project = db
    .prepare(
      `
    INSERT INTO projects (name, type, created_by)
    VALUES (?, ?, ?)
  `,
    )
    .run(projectData.name, projectData.type, projectData.userId);

  // 插入关联的事件
  const eventStmt = db.prepare(`
    INSERT INTO calendar_events (title, project_id, created_by)
    VALUES (?, ?, ?)
  `);

  projectData.events.forEach((event) => {
    eventStmt.run(event.title, project.lastInsertRowid, projectData.userId);
  });

  return project.lastInsertRowid;
});

// 执行事务
try {
  const projectId = createProject(projectData);
} catch (error) {
  // 事务自动回滚
  console.error("创建项目失败:", error);
}
```

### 预编译语句

```typescript
// 提高性能，避免重复编译
const getUserStmt = db.prepare("SELECT * FROM users WHERE id = ?");

const user1 = getUserStmt.get(1);
const user2 = getUserStmt.get(2);
```

## 数据迁移

### 迁移脚本示例

```typescript
// server/db/migrations/001_add_user_status.ts
export const up = (db: Database) => {
  db.exec(`
    ALTER TABLE users ADD COLUMN is_active BOOLEAN DEFAULT 1;
    CREATE INDEX idx_users_is_active ON users(is_active);
  `);
};

export const down = (db: Database) => {
  db.exec(`
    DROP INDEX idx_users_is_active;
    ALTER TABLE users DROP COLUMN is_active;
  `);
};
```

### 运行迁移

```typescript
import { runMigrations } from "./db/migrations";

// 应用所有迁移
runMigrations(db);
```

## 数据备份与恢复

### 备份数据库

```bash
# 完整备份
pg_dump -U postgres yulilog_worklog > data/backup/worklog_$(date +%Y%m%d).sql

# 导出 SQL
pg_dump -U postgres yulilog_worklog > backup.sql
```

### 恢复数据库

```bash
# 从备份恢复
psql -U postgres yulilog_worklog < data/backup/worklog_20260122.sql

# 从 SQL 恢复
psql -U postgres yulilog_worklog < backup.sql
```

### 自动滚动备份

执行脚本：

```bash
bash scripts/automatic-backup.sh
```

脚本默认将备份写入桌面统一换机目录的 `YuliLog换机备份/02-自动更新备份/`，同时处理：

- 生产模式 PostgreSQL（关系型数据库）容器 `yulilog-postgres-prod`
- 开发模式 PostgreSQL（关系型数据库）容器 `yulilog-worklog-postgres`
- 生产模式独立的 `uploads/production/` 上传目录
- 开发模式独立的 `uploads/development/` 上传目录
- 旧共享目录中无法根据数据库判断归属的 `uploads/unassigned/` 历史文件目录
- `.env`、当前代码、未提交修改和部署配置

目录结构：

```text
YuliLog换机备份/
├── 01-历史基准备份-20260714/ # 已完整校验且固定保留的历史底稿
├── 02-自动更新备份/
│   ├── latest/                  # 当前最新可恢复副本
│   │   ├── production.dump     # 生产数据库
│   │   ├── development.dump    # 开发数据库
│   │   ├── uploads/
│   │   │   ├── production/    # 生产上传文件增量副本
│   │   │   ├── development/   # 开发上传文件增量副本
│   │   │   ├── unassigned/    # 未归属历史文件，不挂载到应用
│   │   │   ├── 上传目录分离状态.txt
│   │   │   └── 上传日期分类状态.txt
│   │   ├── project/            # 当前项目与配置副本
│   │   ├── SHA256SUMS           # 数据库 SHA-256（文件校验算法）校验值
│   │   └── 备份状态.txt         # 最新数据快照时间、数据库大小和附件数量
│   ├── history/年/月/日/
│   │   ├── database/           # 每小时数据库快照，保留 30 天
│   │   ├── project/            # 每日项目压缩包，保留 14 天
│   │   └── full/               # 每周完整封装快照，保留约 5 周
│   ├── logs/年/月/日.log        # 自动备份执行日志
│   └── 移动硬盘目标.txt          # 可选，第一行填写移动硬盘备份路径
└── 换机说明.txt                  # 供 Claude 或 Codex 按构建文件联网恢复环境
```

配置移动硬盘后，在 `移动硬盘目标.txt` 第一行写入目标路径，例如：

```text
/Volumes/YuliLogBackup/YuliLog换机备份
```

脚本会从 `docs/YuliLog换机重建说明.txt` 自动覆盖更新备份根目录中唯一的 `换机说明.txt`，并在文档末尾写入更新时间及当前构建文件的 SHA-256（文件校验算法）校验值。构建文件或依赖清单发生变化后，下一次成功备份会直接更新这一份说明，不单独保留说明的历史版本。容器镜像不再单独导出；换机时由 Claude（开发助手）或 Codex（开发助手）读取该说明，并依据备份项目中的 Dockerfile（容器构建文件）、编排文件及依赖清单联网重建生产、开发和 OCR（文字识别）环境。

生产容器将 `uploads/production/` 挂载为容器内的 `/app/uploads`，开发容器将 `uploads/development/` 挂载为容器内的 `/app/uploads`，因此现有 API（应用程序接口）和数据库文件路径不需要修改。一次性迁移脚本 `scripts/split-upload-directories.sh` 会根据生产与开发数据库中的文件引用划分旧共享文件；两个数据库共同引用的文件分别保留一份，没有数据库引用的历史文件移入 `uploads/unassigned/`，避免混入正式生产目录，同时确保不丢弃旧文件。

各环境内部继续按 `业务类别/年/月/日/文件` 分类，日期取中国时区的实际上传时间，不使用发票开票日期等业务日期。`scripts/migrate-uploads-to-date.sh` 根据旧文件保存时间迁移现有文件，并同步更新生产与开发数据库中的路径引用；`scripts/migrate-backup-history-to-date.sh` 将原有数据库、项目、完整快照和运行日志迁移到年/月/日目录。

目标可用时，整个 `YuliLog换机备份` 会增量同步到移动硬盘。移动硬盘未连接时不会中断本机备份，日志会记录目标不可用。严格意义上的实时容灾仍依赖异机或移动硬盘副本；仅保存在本机桌面的自动备份无法防止本机硬盘整体损坏。

当前服务器已创建名为 `YuliLog每小时自动备份` 的 Codex（人工智能编程助手）本地自动任务，每小时调用一次备份脚本并检查数据库校验结果和容器运行状态。自动任务失败时不会删除上一次成功备份，只报告错误原因。

自动备份在改写 `latest` 前会校验三类持久附件均符合“业务类别/年/月/日/文件”结构；发现未日期化业务附件时立即失败，从而保留上一次成功快照。生产、开发环境的 `temp` 临时处理目录和三类目录中的 `.DS_Store` 系统元数据不属于可恢复业务附件，不写入备份，并会从 `latest` 清除旧残留。第二次附件同步后还会复核备份副本，并实时生成包含“分类状态：已完成”、校验时间和三类附件数量的状态文件。

## 性能优化

### 1. 索引优化

```sql
-- 复合索引
CREATE INDEX idx_worklogs_user_date
  ON worklogs(user_id, log_date);

-- 覆盖索引
CREATE INDEX idx_users_role_active
  ON users(role, is_active);
```

### 2. 查询优化

```typescript
// 使用参数化查询
const stmt = db.prepare("SELECT * FROM users WHERE role = ?");
const admins = stmt.all("admin");

// 限制返回字段
const users = db
  .prepare(
    `
  SELECT id, name, role FROM users
`,
  )
  .all();

// 分页查询
const page = 1;
const pageSize = 20;
const offset = (page - 1) * pageSize;

const users = db
  .prepare(
    `
  SELECT * FROM users
  LIMIT ? OFFSET ?
`,
  )
  .all(pageSize, offset);
```

### 3. 批量操作

```typescript
// 批量插入
const insertMany = async (holidays) =>
  db.transaction(async (client) => {
    for (const h of holidays) {
      await client.query(
        `INSERT INTO holidays (id, date, name, type, year, source_url, created_at, updated_at)
       VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
       ON CONFLICT(date) DO UPDATE SET
         name = excluded.name,
         type = excluded.type,
         source_url = excluded.source_url,
         updated_at = excluded.updated_at`,
        [
          h.id,
          h.date,
          h.name,
          h.type,
          h.year,
          h.source_url,
          h.created_at,
          h.updated_at,
        ],
      );
    }
  });

await insertMany(holidayData);
```

### 4. 节假日数据自动维护

- `holidays` 表仍是日历、日志、请假工作日计算和周报定时任务的统一数据源
- `server/services/holidayUpdater.ts` 会自动发现国务院办公厅年度节假日通知，解析放假日和调休上班日后 `upsert` 到 `holidays`
- 服务启动后自动检查一次，并每天 03:10 检查今年和明年是否缺少数据
- `npm run update:holidays 年份` 保留为手动兜底命令，默认强制刷新指定年份

## 数据完整性

### 外键约束

```sql
-- PostgreSQL 默认启用外键约束，无需 PRAGMA

-- 定义外键
FOREIGN KEY (user_id) REFERENCES users(id)
  ON DELETE CASCADE
```

### 唯一约束

```sql
-- 单字段唯一
CREATE TABLE users (
  email TEXT UNIQUE
);

-- 复合唯一
CREATE TABLE worklogs (
  UNIQUE(user_id, log_date)
);
```

### 检查约束

```sql
CREATE TABLE projects (
  budget REAL CHECK(budget >= 0),
  status TEXT CHECK(status IN ('PLANNING', 'ACTIVE', 'COMPLETED'))
);
```

## 更新日志

- 2026-07-30: 转正申请新增主管、管理员和董事长审批人账号及姓名快照；每版提交固定审批人，历史流程优先读取签署时姓名，不再随当前角色人员变化
- 2026-07-30: 修正“其他请假”默认配置的可重复初始化语句，已有配置时不再因主键重复导致后端重启失败
- 2026-07-30: 用户角色约束新增 `chairman`（董事长）；董事长不建立员工档案且排除工资数据，个人签名改为首次确认后不可覆盖或删除，历史代管签名表停止产生新记录
- 2026-04-10: 补充离职档案聚合查询的数据来源说明，明确离职主表与附件表用于员工详情离职档案展示

## 注意事项

1. **并发控制**：
   - PostgreSQL 支持并发读写
   - 使用 WAL 模式提高并发性能
   - 长事务会阻塞写操作

2. **数据类型**：
   - PostgreSQL 是强类型
   - 建议在应用层严格验证
   - 使用 TEXT 存储 JSON 数据

3. **备份策略**：
   - 定期备份数据库文件
   - 重要操作前手动备份
   - 测试恢复流程

4. **性能监控**：
   - 分析慢查询
   - 定期 VACUUM 清理
   - 监控数据库文件大小

## 更新日志

- 2026-07-27: `resignation_requests.status` 新增 `pending_confirmation`；五类离职档案齐全时从 `draft` 自动迁移到待确认状态，档案不完整时自动回退，只有管理员确认后才写入 `approved` 并联动员工与账号状态
- 2026-04-12: 修复 resignation_templates 表 template_type CHECK 约束不完整导致上传模板失败
  - 原约束仅允许 `application_form`、`handover_form`，与代码支持的 7 种类型不匹配
  - DDL 定义更新为全部 7 种类型，并添加迁移语句 DROP 旧约束 + ADD 新约束
- 2026-04-10: 调整离职模板管理展示范围与员工端页面分区
  - `resignation_templates` 数据结构不变，管理员端前端页面仅展示 5 类补充模板入口
  - 员工端离职页面新增“离职管理｜待我处理”内部视图切换，不涉及表结构调整
- 2026-04-09: 离职材料扩展并按离职原因动态校验
  - `resignation_documents.document_type` 扩展为离职申请表、离职人交接单、交接人交接单、终止/解除劳动关系证明、固定资产交接单、离职经济补偿协议书、离职其他费用结算约定
  - `resignation_templates.template_type` 扩展为离职申请表模板、交接单模板、终止/解除劳动关系证明模板、固定资产交接单模板、离职经济补偿协议书模板、离职其他费用结算约定模板、合伙人离任分红结算模板
  - 离职申请提交时按 `resign_type` 校验员工侧必传材料完整性：主动离职/合同到期要求终止/解除劳动关系证明、固定资产交接单、离职其他费用结算约定；被辞退额外要求离职经济补偿协议书
  - 合伙人离任分红结算当前仅作为管理员可维护模板类型，不纳入本轮离职提交流程必传校验
  - 管理端离职详情可读取必传材料与缺失材料信息，辅助审批核验
- 2026-04-09: 新增离职申请、离职附件、离职模板等人事流程表结构说明
- 2026-04-03: 报销模块数据完整性和安全性增强
  - `deductionOcrCache` 缓存机制：核减发票保存时强制校验服务端识别金额，防止客户端篡改
  - `payment_proof_hashes` 表：修复 fileHash 与 proofNo 索引对应关系，确保去重正确性
  - 发票识别结果校验：强制要求金额、日期、发票号三个关键字段齐全才视为有效发票
  - OCR 识别失败时 `isValidInvoice` 标记为 false，避免污染核减上传结果
- 2026-03-26: PostgreSQL 数值类型与事务一致性修复
  - `server/db/index.ts` 新增 `pg` `int8/numeric` 类型解析，聚合结果统一按 number 返回
  - `server/routes/probation.ts` 审批通过/驳回事务改为同一 `PoolClient` 内执行，确保事务原子性
  - 文档中的 SQLite 备份与 `PRAGMA` 示例已替换为 PostgreSQL 写法
- 2026-03-26: Dockerfile 与 docker-compose 适配 PostgreSQL 容器化部署
  - `Dockerfile` 已适配 PostgreSQL，移除旧数据库依赖
  - 生产镜像默认数据库环境变量改为 `DATABASE_URL`
  - `docker-compose.yml` 新增 `postgres` 服务、健康检查与数据卷，应用服务通过 `DATABASE_URL` 连接 PostgreSQL
- 2026-03-26: 报销路由适配 PostgreSQL 查询与事务接口
  - `server/routes/reimbursement.ts` 全量切换为异步 `db.prepare(...).get/all/run` 调用
  - 创建、更新、撤回、删除、恢复等事务性写操作统一改为 `db.transaction(async (client) => ...)` + `client.query` 风格封装
  - 发票分类聚合改为 PostgreSQL `STRING_AGG(DISTINCT ..., ',')`，报销软删除字段读写改为 boolean 语义
- 2026-03-26: 审批中心路由适配 PostgreSQL 查询接口
  - `server/routes/approval.ts` 全量切换为异步 `db.prepare(...).get/all/run` 调用
  - 发票分类聚合改为 PostgreSQL `STRING_AGG(DISTINCT ..., ',')`
  - 报销软删除字段判断改为 PostgreSQL boolean 语义
- 2026-03-12: 安全修复 - 补齐 User 类型中 employee_no 字段定义（server/types/database.ts）
- 2026-01-28: 报销单表增加软删除支持
  - 新增 `is_deleted` 字段（INTEGER DEFAULT 0）标记是否删除
  - 新增 `deleted_at` 字段（TEXT）记录删除时间
  - 用户删除报销单时不再物理删除，改为软删除
  - 管理员在审批中心可查看所有历史报销数据（包括已删除）
- 2026-01-22: 创建文档，描述数据库模块核心功能
- 2026-04-02: 财务区数据库迁移 - payment_batches 表新增 proof_hash_set 字段（TEXT）及唯一索引，用于防止付款回单重复提交
- 2026-04-12: employee_profiles 表新增 contract_end_date 字段（TEXT），存储合同到期日期，根据 hire_date 自动计算（+1年）；数据库初始化时自动迁移回填已有记录
- 2026-04-12: 新增 probation_history 表，记录管理员将员工改回实习期时归档的历史转正记录（含入职日期、试用期截止、审批状态、重置原因等）
- 2026-06-02: 新增节假日自动更新服务，自动解析国务院年度通知并维护 `holidays` 表
- 2026-07-14: 新增生产与开发模式自动滚动备份脚本和 Codex（人工智能编程助手）本地自动任务，每小时更新数据库、上传文件和项目配置，并支持移动硬盘增量同步
- 2026-07-14: 将手动完整备份与自动更新备份整理到桌面统一换机目录，换电脑时只需复制整个 `YuliLog换机备份` 目录
- 2026-07-14: 自动备份新增每周完整封装快照并保留约 5 周，固定基准备份重命名为历史基准备份以明确回退用途
- 2026-07-14: 自动备份新增容器镜像变化检测，镜像变化时自动导出离线镜像并保留最新两个版本，同时将整个换机备份增量同步到移动硬盘
- 2026-07-14: 修复首次执行容器镜像备份时父目录尚未创建导致锁目录初始化失败的问题
- 2026-07-14: 为降低换机备份体积，停止导出容器离线镜像，改为自动更新换机说明并由开发助手按项目构建文件联网恢复生产、开发及 OCR（文字识别）依赖
- 2026-07-14: 换机说明新增构建文件校验值和更新时间，每小时备份时自动刷新，依赖变化后无需手工维护说明版本
- 2026-07-14: 换机说明改为仅保留备份根目录中的一个最新版，每小时直接覆盖更新，不再在 latest 中保留重复副本
- 2026-07-14: 将生产与开发上传文件改为独立物理目录，旧共享文件和符号链接按两个数据库引用迁移，未归属历史项单独保留，清理旧共享空目录，并同步调整自动备份与换机恢复说明
- 2026-07-15: 上传文件统一按实际上传日期保存到业务类别/年/月/日目录，现有文件迁移时同步更新两个数据库路径，历史备份和运行日志也改为年/月/日目录
- 2026-07-15: 备份历史迁移遇到同日新旧项目快照时分别保留并标记，旧版与新版同日日志按时间顺序合并，避免覆盖历史记录
- 2026-07-15: 自动备份新增持久附件日期结构前置与落盘后校验，排除生产、开发临时处理文件和系统元数据，并实时刷新分类完成状态及附件数量
- 2026-07-15: 新增带薪事假幂等迁移，将“999 天且不校验余额”的旧版配置改为每年 3 天，并同步不存在使用或预占冲突的历史余额
- 2026-07-15: `leave_requests` 新增跨年余额分配和余额规则快照字段，请假提交、审批及管理员余额调整使用事务行锁保证余额不透支
- 2026-07-15: 为 `probation_confirmations.employee_id` 增加唯一索引，入职提交与转正记录创建改为同一事务，避免重复或半完成数据
- 2026-07-15: 离职表结构补齐驳回对象、交接驳回状态和材料当前版本字段，审批状态、员工状态与审计日志采用事务一致性写入
- 2026-07-15: 创建用户时使用事务级并发锁统一写入账号、员工草稿档案和待转正记录，员工编号在锁内生成，避免重复编号及半完成账号
- 2026-07-15: 转正提交和终审使用事务行锁串行处理同一转正记录与审批实例，确保申请状态、审批记录和员工在职状态一致
- 2026-07-15: 管理员将员工改回实习期时，历史归档、旧审批清理、新转正记录及员工状态改为单一事务；旧物理文件仅在提交成功后清理
- 2026-07-16: 新增 `employee_salary_profiles` 邀请函工资档案表和 `payroll_records` 月度工资快照表，金额统一使用精确 `NUMERIC`（精确数值）类型
- 2026-07-16: 清理并持续排除超级管理员系统账号的月度工资记录
- 2026-07-16: 员工编号改为管理员创建或编辑用户时填写，移除数据库启动自动补号，并仅将账号中的非空编号同步到员工档案
- 2026-07-27: 创建用户时恢复事务内自动生成员工编号，取 `users` 与 `employee_profiles` 已有有效 `YULI-CS` 编号最大数字序号后继续递增
- 2026-07-16: 员工合同档案新增起止日期字段并支持续签历史；合同到期时间改为取最新合同，清理按入职日期推算的旧值
- 2026-07-16: 正式邀请函工资同步支持重置当前及未来月份的本月工资手工标记，同时保留缴费基数和个税手工值
- 2026-07-16: `employee_documents` 新增“其他”业务类型，保存固定十类之外的员工资料，不计入必备档案完成度
- 2026-07-16: 人事档案支持事务式一键删除，清空合同到期时间但保留邀请函工资基线和历史月度工资记录
- 2026-07-16: `users.employee_no` 增加非空条件唯一索引，用户编号修改与员工基础档案记录同步写入，并在启动时修复历史不一致编号
- 2026-07-16: 用户编号修改不再更新人事档案展示文件名，档案下载接口直接返回管理员上传的原始内容；动态编号仅用于入职模板
- 2026-07-16: 人事核心状态和数值增加数据库检查约束，工资记录增加版本列及字段级修改日志；迁移按约束类型隔离，避免单条旧脏数据阻断必要字段升级
- 2026-07-17: 请假审批人统一为总经理，启动时逐条迁移普通管理员名下的历史待办，并为总经理本人申请保留其他总经理或超级管理员兜底
- 2026-07-17: `leave_requests.status` 增加 `draft`（草稿）；撤回释放余额后转为草稿，草稿支持整条申请链硬删除，并在每月 1 日自动清理上月遗留草稿
- 2026-07-17: 数据库启动迁移将旧版 `cancelled` 请假记录统一转换为 `draft`，保留撤回时间和历史链并修正 `balance_reserved`
- 2026-07-23: `employee_documents` 新增 `probation_end_date` 字段，保存劳动合同中识别出的试用期截止日期；续签合同无试用期时保持为空
- 2026-07-23: `probation_documents` 增加员工关联并允许转正记录为空，支持直接在职员工补录转正申请表且不生成虚假审批记录
- 2026-07-23: `employee_profiles.hire_date` 改为最早识别合同开始日期的系统快照；启动时回填已有合同数据，合同删除时重算或清空
- 2026-07-23: 所有状态转正记录的试用期截止改为第一份劳动合同的识别值并允许为空；启动迁移同步修正已有续签员工
- 2026-07-23: 启动迁移清理无可识别劳动合同员工的旧入职、合同到期及转正日期；删除最后一份合同时同步清空
- 2026-07-23: `employee_profiles` 新增四个合同模板拟定日期字段，独立于正式合同识别日期保存
- 2026-07-24: `employee_profiles` 新增上一期合同模板期限、试用期及职位快照字段；正式合同归档前保存快照，启动时按最近正式合同补齐已有数据
- 2026-07-24: 转正主表新增在线申请版本、当前签署阶段、转正类型、本人述职、员工信息快照、指定主管和正式文件生成时间；新增四阶段电子签名记录表，正式转正文件增加系统生成来源及申请版本关联；历史已转正记录统一补齐为已完成阶段
- 2026-07-24: 新增 `user_signatures` 个人电子签名表，每个账号保存一份当前签名路径；转正流程继续按申请版本保存独立签名快照
- 2026-07-24: 新增 `user_delegated_signatures` 管理员代管总经理签名表；转正签名记录增加签名类型和签名人姓名，区分实际操作管理员与申请单签名人
- 2026-07-24: 离职数据模型改为管理员归档模式，新增五类模板和档案类型、当前版本标识及 `resignation_template_drafts` 在线编辑草稿表；档案齐全后事务联动离职记录、员工状态和原账号状态
- 2026-07-24: `resignation_template_drafts` 新增 `overlay_json`，按离职记录、模板类型和模板版本保存自动字段修改值及自定义文字坐标，原模板文件保持不变
- 2026-07-27: `probation_history` 增加表单版本、归档阶段、审批记录快照和签署历史快照字段；员工被重置为实习期前先保存完整历史明细，再清理原转正流程数据
- 2026-07-27: `probation_documents.source_type` 新增 `official` 正式盖章件；系统生成的 `generated` 文件保留为待盖章底稿，历史管理员归档件按上传角色和转正状态自动迁移
- 2026-07-27: `resignation_templates` 新增上传收紧为真实 PDF（便携式文档格式），不迁移或删除历史 DOCX（开放 XML 文字处理文档）记录
- 2026-07-27: 离职状态联动改为管理员确认触发；五类档案齐全后保持 `draft`，确认时才在同一事务中写入已完成、已离职、账号停用和审计日志
- 2026-07-27: 年假余额改为所有活动账号至少使用类型配置的基础额度，有有效入职日期时再按工龄上调；启动迁移同步修正旧版年假说明
- 2026-07-27: 请假审批人收口为申请人之外的活动总经理，取消超级管理员兜底；启动迁移仅向总经理转交历史待办，管理员和超级管理员继续只读抄送
- 2026-07-28: 用户角色数据库约束新增 `boss`（BOSS），既有数据库在启动时自动更新角色检查约束。
- 2026-07-28: BOSS账号允许联系方式、组织和员工编号为空，不创建员工档案；工资及经营人员统计按角色排除BOSS。
- 2026-07-28: BOSS角色仅调整显示名称，数据库角色编码继续保持 `boss`。
- 2026-07-29: 新增 `human_cost_receipts` 月度人力成本回单表，保存四类银行回单原件、文件摘要、异步识别状态、识别笔数和精确汇总金额。
- 2026-07-29: 新增 `human_cost_receipt_items` 工资单笔回单关联表，保存员工匹配、金额、来源页码及裁切位置；汇总表增加非工资回单忽略笔数。
- 2026-07-29: `payroll_records` 新增公积金缴费基数及手工修改标记，工资变更日志增加对应字段；原 `contribution_base` 明确作为社保缴费基数。
- 2026-07-29: 个人个税明细识别结果直接回填月度工资记录的应纳个税并沿用手工标记、版本和字段修改日志；上传原件处理后清理，不新增持久化表。
- 2026-07-29: 新增 `payroll_tax_detail_files` 月度个税明细文件表，持久化保存识别原件和结果摘要，支持同月替换及在线预览。
- 2026-07-30: `leave_requests` 新增 `approval_notice_unread` 字段，持久化员工请假审批通过结果的未读状态
- 2026-07-30: 上一期合同模板日期和职位字段调整为仅保留历史兼容，不再作为最新版空白合同模板的预览数据来源
- 2026-07-30: `leave_requests` 新增申请业务类型、组合批次和主申请关联字段及索引，分离组合/续补假关系与驳回重提版本链
- 2026-07-31: `leave_requests` 新增 `rejection_notice_unread` 字段及索引，持久化员工按条查看请假驳回提醒的状态
- 2026-07-31: 组合续假和组合补假复用 `combination_group_id` 与 `parent_request_id` 建立批次及主申请关系；婚假复用员工身份证号并通过事务级锁保证一次性申请资格
- 2026-07-31: 请假审批人按申请人角色重新分配；普通员工待办绑定活动总经理，总经理待办绑定活动董事长，启动迁移同步修正历史审批中记录
- 2026-07-31: 开发与生产数据库继续保持数据隔离；启动迁移为既有生产库幂等补齐转正申请备注字段，并在迁移后校验业务必需字段，缺失时阻止服务带病启动
- 2026-07-31: 日志评论字段与周报锁定字段迁移统一改为幂等加列，已有字段不再产生重复迁移错误日志
- 2026-08-04: `leave_requests.request_no` 从历史 `LR` 前缀幂等迁移为 `QJ`，新申请按 `QJ-YYYY-NNNNN` 生成；请假查询中的总经理和董事长审批身份改为账号角色优先
