# 10 - 报销管理模块

## 模块概述

报销管理模块负责处理员工的日常报销申请，包括基础报销、大额报销和商务报销。支持发票上传、OCR自动识别、PDF压缩等功能。

## 主要功能

### 1. 基础报销
- 报销单创建与编辑
- 报销类型选择（行政办公、三方服务、业务提升）
- 发票上传与OCR识别
- 报销金额和日期自动填充
- 草稿保存与提交审批

### 2. 发票处理
- PDF格式验证
- 文件大小限制（5MB）
- 超大文件自动压缩
- 发票OCR自动识别
- 发票文件存储管理

### 3. OCR识别
- 百度OCR集成（主要方案）
- 腾讯云OCR集成（备用方案）
- 自动识别报销金额（价税合计）
- 自动识别开票日期
- 自动识别发票号码
- 识别结果自动填充后不可手动修改

## 核心文件

### 前端文件 - 页面层
- [src/views/BasicReimbursement.vue](../src/views/BasicReimbursement.vue) - 基础报销列表页面
- [src/views/BasicReimbursementDetail.vue](../src/views/BasicReimbursementDetail.vue) - 基础报销详情/创建页面
- [src/views/LargeReimbursement.vue](../src/views/LargeReimbursement.vue) - 大额报销列表页面
- [src/views/LargeReimbursementDetail.vue](../src/views/LargeReimbursementDetail.vue) - 大额报销详情/创建页面
- [src/views/BusinessReimbursement.vue](../src/views/BusinessReimbursement.vue) - 商务报销列表页面
- [src/views/ReimbursementStatistics.vue](../src/views/ReimbursementStatistics.vue) - 报销统计页面

### 前端文件 - 组件层（UI组件）
- [src/components/reimbursement/InvoiceUploader.vue](../src/components/reimbursement/InvoiceUploader.vue) - 发票上传组件
- [src/components/reimbursement/InvoiceTable.vue](../src/components/reimbursement/InvoiceTable.vue) - 发票明细表格组件
- [src/components/reimbursement/TypeSelector.vue](../src/components/reimbursement/TypeSelector.vue) - 报销类型选择器组件

### 前端文件 - 逻辑层（Composables）
- [src/composables/reimbursement/useInvoice.ts](../src/composables/reimbursement/useInvoice.ts) - 发票操作逻辑（上传、删除、OCR识别）
- [src/composables/reimbursement/useInvoiceValidation.ts](../src/composables/reimbursement/useInvoiceValidation.ts) - 发票校验逻辑（日期、重复、金额）
- [src/composables/reimbursement/useReimbursement.ts](../src/composables/reimbursement/useReimbursement.ts) - 报销单CRUD逻辑
- [src/composables/reimbursement/index.ts](../src/composables/reimbursement/index.ts) - 统一导出

### 前端文件 - 工具层（常量）
- [src/utils/reimbursement/constants.ts](../src/utils/reimbursement/constants.ts) - 报销类型、状态等常量定义

### 后端文件
- [server/routes/reimbursement.ts](../server/routes/reimbursement.ts) - 报销相关API路由

## 模块化架构

### 分层结构

```
src/
├── views/                          # 页面层 - 组装组件和逻辑
│   ├── BasicReimbursementDetail.vue
│   └── LargeReimbursementDetail.vue
├── components/reimbursement/       # 组件层 - 纯UI展示
│   ├── InvoiceUploader.vue         # 发票上传
│   ├── InvoiceTable.vue            # 发票表格
│   └── TypeSelector.vue            # 类型选择
├── composables/reimbursement/      # 逻辑层 - 状态和业务逻辑
│   ├── useInvoice.ts               # 发票操作
│   ├── useInvoiceValidation.ts     # 发票校验
│   └── useReimbursement.ts         # 报销单CRUD
└── utils/reimbursement/            # 工具层 - 常量和纯函数
    └── constants.ts                # 常量定义
```

### 分区开发指南

| 修改内容 | 只需修改的文件 | 不会影响 |
|---------|---------------|---------|
| 发票校验规则（日期、重复等） | `useInvoiceValidation.ts` | UI组件、页面布局 |
| 发票上传/删除/OCR逻辑 | `useInvoice.ts` | 校验逻辑、表格组件 |
| 发票上传UI样式 | `InvoiceUploader.vue` | 校验逻辑、表格组件 |
| 发票表格UI样式 | `InvoiceTable.vue` | 上传组件、校验逻辑 |
| 报销类型选项 | `constants.ts` + `TypeSelector.vue` | 发票相关逻辑 |
| 报销单API交互 | `useReimbursement.ts` | UI组件、发票逻辑 |
| 基础报销页面布局 | `BasicReimbursementDetail.vue` | 所有子组件和逻辑 |
| 大额报销页面布局 | `LargeReimbursementDetail.vue` | 所有子组件和逻辑 |

### 组件职责

**InvoiceUploader.vue** - 发票上传组件
- Props: `modelValue`(文件列表), `maxFiles`(最大数量), `disabled`(禁用), `themeColor`(主题色)
- Events: `file-change`, `delete-file`, `exceed`
- 职责: 纯UI展示，不包含业务逻辑

**InvoiceTable.vue** - 发票明细表格
- Props: `invoiceList`(发票列表), `readonly`(只读), `themeColor`(主题色), `amountThreshold`(金额阈值), `showThresholdWarning`(显示警告), `showDeduction`(显示核减金额列)
- Events: `delete`
- 职责: 展示发票明细，计算总金额、核减金额和实际报销金额

**TypeSelector.vue** - 报销类型选择器
- Props: `modelValue`(选中值)
- Events: `select`
- 职责: 展示报销类型选项，处理选择

### Composable 职责

**useInvoice** - 发票操作
- 状态: `invoiceList`, `fileList`, `totalAmount`
- 方法: `handleFileChange`, `deleteInvoiceByFile`, `deleteInvoiceById`, `loadInvoices`, `getInvoicesForSubmit`

**useInvoiceValidation** - 发票校验
- 函数: `validateInvoiceDate`, `validateInvoiceDuplicate`, `validateInvoiceAmount`, `validateFile`
- 发票日期校验规则：
  - ✅ 可以上传当年的发票（1月到12月）
  - ✅ 当年1月份可以上传去年12月的发票
  - ❌ 当年1月份不能上传去年11月及之前的发票
  - ❌ 其他月份不能上传跨年发票
  - ❌ 不能上传未来月份的发票

**useReimbursement** - 报销单CRUD
- 状态: `pageMode`, `isReadonly`, `reimbursementMonth`, `loading`
- 方法: `saveDraft`, `submitForApproval`, `loadReimbursementDetail`, `goBack`, `generateTitle`

## 报销类型

### 行政办公
- 办公设备 (office_equipment)
- 办公器具 (office_tools)
- 办公耗材 (office_supplies)
- 劳保用品 (labor_protection)
- 水电费 (utilities)
- 物业费 (property_fee)
- 清洁费 (cleaning_fee)
- 差旅费 (travel_expense)
- 茶水费 (refreshment)

### 三方服务
- 图文服务 (graphic_service)
- 快递服务 (express_service)
- 车辆服务 (vehicle_service)
- 物流服务 (logistics_service)
- 公证服务 (notary_service)
- 咨询服务 (consulting_service)
- 技术服务 (technical_service)
- 制证服务 (certificate_service)

### 业务提升
- 培训费 (training_fee)
- 知识付费 (knowledge_payment)
- 专家指导 (expert_guidance)

## API 接口

| 接口 | 方法 | 说明 | 权限 |
|------|------|------|------|
| `/api/reimbursement/upload-invoice` | POST | 上传发票并进行OCR识别 | 需要登录 |
| `/api/reimbursement/compress-pdf` | POST | 压缩PDF文件 | 需要登录 |

### 上传发票接口

**请求**：
```typescript
POST /api/reimbursement/upload-invoice
Content-Type: multipart/form-data

{
  invoice: File // PDF文件
}
```

**响应**：
```typescript
{
  success: true,
  message: '发票上传成功',
  data: {
    fileName: 'invoice-1234567890-发票.pdf',
    filePath: '/uploads/invoices/invoice-1234567890-发票.pdf',
    compressed: false, // 是否经过压缩
    ocrResult: {
      amount: 350.50,
      date: '2026-01-23',
      invoiceNumber: '12345678',
      seller: '北京某某公司'
    }
  }
}
```

### PDF压缩接口

**请求**：
```typescript
POST /api/reimbursement/compress-pdf
Content-Type: multipart/form-data

{
  pdf: File // PDF文件
}
```

**响应**：
```
Content-Type: application/pdf
Content-Disposition: attachment; filename="compressed-原文件名.pdf"

[压缩后的PDF文件二进制数据]
```

## 使用流程

### 创建报销单流程

1. **选择报销类型**
   - 用户点击"新建报销单"按钮
   - 进入类型选择页面
   - 点击对应的报销类型卡片
   - 自动进入下一步

2. **填写报销信息**
   - 系统自动显示当前报销月份（格式：YYYY年MM月）
   - 显示已选择的报销类型
   - 上传发票PDF文件（仅支持PDF，单个文件不超过5M）
   - 系统自动进行OCR识别
   - 自动填充报销金额、开票日期和发票号码（识别后不可手动修改）
   - 用户补充详细说明
   - 保存草稿或提交审批

3. **文件处理**
   - 文件格式验证（仅支持PDF，JPG/PNG等格式会提示"请上传正确格式的发票"）
   - 文件大小检查（5MB限制，超过会提示错误）
   - OCR识别发票信息
   - 文件存储到服务器
   - 上传后显示PDF文档图标预览

## OCR配置

### 本地 OCR 识别

系统使用**本地 OCR** 进行发票识别，具有以下特点：

- ✅ **数据安全**：所有识别在本地完成，发票数据不会上传到外部服务器
- ✅ **无需配置**：开箱即用，无需申请任何 API 密钥
- ✅ **零成本**：完全免费，无调用次数限制
- ⚠️ **准确率**：识别准确率可能低于云端服务，建议人工核对

### OCR 技术栈

- **Tesseract.js**：开源 OCR 引擎，支持中英文识别
- **PDF 解析**：使用 pdf-lib 进行 PDF 文件处理
- **正则匹配**：智能提取发票关键信息（金额、日期、发票号码等）

### 识别流程

1. 用户上传 PDF 发票
2. 系统在本地解析 PDF 文件
3. 使用正则表达式提取关键信息
4. 返回识别结果供用户确认
5. 用户可手动修改识别结果

### 注意事项

- 本地 OCR 适用于标准格式的电子发票
- 对于扫描件或特殊格式发票，建议手动填写
- 系统会自动标记识别结果的可信度
- 所有识别结果都需要用户最终确认

## 前端实现

### 文件上传组件

```vue
<el-upload
  v-model:file-list="fileList"
  action="#"
  :auto-upload="false"
  :limit="5"
  :before-upload="beforeUpload"
  :on-change="handleFileChange"
  :on-exceed="handleExceed"
  :on-preview="handlePreview"
  accept=".pdf"
  list-type="picture-card"
  class="invoice-upload"
>
  <el-icon><Plus /></el-icon>
  <template #file="{ file }">
    <div class="invoice-preview">
      <el-icon class="pdf-icon"><Document /></el-icon>
      <span class="file-name">{{ file.name }}</span>
    </div>
  </template>
</el-upload>
```

### 文件上传处理

```typescript
const handleFileChange = async (file: any, fileList: any[]) => {
  // 验证文件格式 - 严格检查
  const fileName = file.name?.toLowerCase() || ''
  const isImageFile = fileName.endsWith('.jpg') || fileName.endsWith('.jpeg') ||
                      fileName.endsWith('.png') || fileName.endsWith('.gif') ||
                      fileName.endsWith('.bmp')

  if (isImageFile) {
    ElMessage.error('请上传正确格式的发票')
    // 移除非PDF文件
    const index = fileList.findIndex(f => f.uid === file.uid)
    if (index > -1) {
      fileList.splice(index, 1)
    }
    return
  }

  if (file.raw && file.raw.type !== 'application/pdf') {
    ElMessage.error('仅支持PDF文件，单个文件不超过5M')
    return
  }

  // 调用后端API进行发票OCR识别
  try {
    ElMessage.info('正在识别发票信息...')

    const formData = new FormData()
    formData.append('invoice', file.raw)

    const response = await fetch('/api/reimbursement/upload-invoice', {
      method: 'POST',
      body: formData,
      credentials: 'include',
    })

    const result = await response.json()

    if (result.success && result.data.ocrResult) {
      const { amount, date, invoiceNumber } = result.data.ocrResult

      // 自动填充识别结果
      formData.amount = amount
      formData.invoiceDate = date
      formData.invoiceNumber = invoiceNumber
      formData.ocrFilled = true // 标记为OCR填充，禁止手动修改

      ElMessage.success('发票识别成功！已自动填充报销金额、开票日期和发票号码')
    }
  } catch (error) {
    ElMessage.error('发票识别失败，请手动填写信息')
  }
}
```

## 后端实现

### PDF压缩功能

```typescript
async function compressPDF(inputPath: string, outputPath: string): Promise<void> {
  const existingPdfBytes = fs.readFileSync(inputPath)
  const pdfDoc = await PDFDocument.load(existingPdfBytes)

  // 移除元数据以减小文件大小
  pdfDoc.setTitle('')
  pdfDoc.setAuthor('')
  pdfDoc.setSubject('')
  pdfDoc.setKeywords([])
  pdfDoc.setProducer('')
  pdfDoc.setCreator('')

  // 保存压缩后的PDF
  const pdfBytes = await pdfDoc.save({
    useObjectStreams: false,
  })

  fs.writeFileSync(outputPath, pdfBytes)
}
```

### 百度OCR识别

```typescript
async function recognizeInvoiceWithBaidu(filePath: string) {
  // 1. 获取access_token
  const tokenResponse = await axios.post(
    'https://aip.baidubce.com/oauth/2.0/token',
    null,
    {
      params: {
        grant_type: 'client_credentials',
        client_id: process.env.BAIDU_OCR_API_KEY,
        client_secret: process.env.BAIDU_OCR_SECRET_KEY,
      },
    }
  )

  const accessToken = tokenResponse.data.access_token

  // 2. 读取PDF文件并转换为base64
  const fileBuffer = fs.readFileSync(filePath)
  const base64Image = fileBuffer.toString('base64')

  // 3. 调用增值税发票识别API
  const ocrResponse = await axios.post(
    `https://aip.baidubce.com/rest/2.0/ocr/v1/vat_invoice?access_token=${accessToken}`,
    `pdf_file=${encodeURIComponent(base64Image)}&pdf_file_num=1`,
    {
      headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
      },
    }
  )

  const result = ocrResponse.data.words_result

  // 4. 解析识别结果
  return {
    amount: parseFloat(result.AmountInFiguers || result.TotalAmount || '0'),
    date: result.InvoiceDate || new Date().toISOString().split('T')[0],
    invoiceNumber: result.InvoiceNum || result.InvoiceCode,
    seller: result.SellerName,
  }
}
```

## 文件存储

### 目录结构

```
uploads/
├── temp/              # 临时文件目录
│   └── compressed-*.pdf
└── invoices/          # 发票文件目录
    └── invoice-*.pdf
```

### 文件命名规则

- 临时文件：`compressed-{timestamp}.pdf`
- 发票文件：`invoice-{timestamp}-{原文件名}.pdf`

### 文件访问

发票文件通过静态文件服务访问：
```
http://localhost:3000/uploads/invoices/invoice-1234567890-发票.pdf
```

## 注意事项

1. **文件格式**：
   - 仅支持PDF格式
   - JPG、PNG等图片格式会提示"请上传正确格式的发票"
   - 前端和后端都进行格式验证

2. **文件大小**：
   - 单个文件最大5MB
   - 超过5MB会提示错误，不允许上传

3. **OCR识别**：
   - 需要配置百度OCR或腾讯云OCR
   - 未配置时使用模拟数据
   - 识别字段包括：报销金额、开票日期、发票号码
   - 识别结果自动填充后不可手动修改

4. **报销月份**：
   - 自动显示当前月份（格式：YYYY年MM月）
   - 显示在报销类型上方

5. **发票预览**：
   - 上传后显示PDF文档图标
   - 双击缩略图可在新窗口预览发票
   - 双击时不会选中文字

6. **安全性**：
   - 所有接口需要登录认证
   - 文件上传使用multer中间件
   - 防止路径遍历攻击

7. **错误处理**：
   - 文件上传失败自动清理临时文件
   - OCR识别失败提示用户重新上传
   - 格式错误自动移除文件

## 更新日志

- 2026-03-12: 优化审批中心待办列表和统计区域
  - 待办列表显示除已完成外的所有状态（待审批、待付款、付款中、待确认收款）
  - 待办排序：待审批→待支付→待确认
  - 统计方块改为：待审批、基础报销、大额报销、商务报销，显示当月数量和金额
  - 审批通过后操作按钮从"已通过待付款"标签改为"付款"按钮
- 2026-03-12: 优化总经理审批流程展示
  - 每个状态只显示已完成的步骤和下一步，不再显示全部未来节点
  - pending：仅显示”员工提交”和”总经理审批”
  - approved：显示到”财务付款(待处理)”
  - paying：显示到”上传付款凭证(待处理)”
  - payment_uploaded：显示到”确认收款(待确认)”
  - completed：显示全部已完成步骤
  - rejected：仅显示”员工提交”和”总经理审批(驳回)”
- 2026-03-13: 管理员待办列表新增筛选功能
  - 支持按员工、类型（基础/大额）、状态（待审批/待付款/付款中/待确认收款）、日期范围筛选
  - 新增查询和重置按钮
  - 后端 /api/approval/pending 接口支持 userId、type、status、startDate、endDate 参数
- 2026-03-13: 审批中心待办/待付款/已付款列表新增报销类型和报销范围/区域列
  - 移除原”类型”列，在”标题/金额”后新增”报销类型”（发票分类）和”报销范围/区域”两列
  - 报销类型显示发票的具体分类（如运输服务、汽油、无票报销等），从发票表聚合去重
  - 后端三个接口新增 invoiceCategories、reimbursementScope 字段返回
- 2026-03-13: 将财务区所有”已拒绝”/”拒绝”字眼统一改为”已驳回”/”驳回”
- 2026-03-12: 优化管理员发票管理金额显示
  - 发票金额列仅在报销单状态为付款中/待确认/已完成时显示
  - 汇总栏（总金额、实报金额、核减金额）仅统计已付款的发票
- 2026-03-11: 新增总经理审批中心"全部查询"功能
  - 在总经理审批中心添加"全部查询"标签页
  - 支持按报销范围（区域）筛选商务报销
  - 可选择海淀区、朝阳区、公司内部等范围进行查询
  - 显示所有已提交的商务报销（不包括草稿）
  - 新增后端API：/api/approval/gm-all（支持scope参数筛选）
  - 更新统计接口，添加allCount字段
- 2026-03-11: 新增总经理审批中心
  - 创建总经理专用审批中心页面（GMApprovalCenter.vue）
  - 只显示商务报销的审批内容
  - 位于财务区报销统计下方
  - 包含待审批和本月已审批两个标签页
  - 新增后端API：/api/approval/gm-statistics、/api/approval/gm-pending、/api/approval/gm-completed
  - 管理员审批中心不再显示商务报销的待审批项
- 2026-03-11: 优化管理员审批中心
  - 商务报销不再出现在管理员的待审批列表中
  - 管理员只负责商务报销的付款操作
  - 商务报销审批由总经理负责
- 2026-01-23: 创建报销管理模块文档，实现发票上传、OCR识别和PDF压缩功能
- 2026-01-25: 优化报销窗口功能
  - 新增报销月份字段，自动显示当前月份
  - 修改发票上传提示为"仅支持PDF文件，单个文件不超过5M"
  - 添加文件格式自动识别，JPG/PNG等格式提示"请上传正确格式的发票"
  - 发生日期改为开票日期
  - 新增发票号码字段
  - OCR识别内容填充后设置为只读，不可手动修改
  - 优化发票上传预览，显示PDF文档图标和文件名
  - **支持多张发票上传**：一个报销项可上传多张发票，支持以下功能：
    - 发票明细表格显示：序号、报销金额、开票日期、发票号码
    - 报销金额自动相加计算总计
    - 开票日期显示为中文格式（如：2026年1月2日）
    - 支持从表格或文件预览处删除单个发票
  - **发票重复检测**：根据发票号码自动检测重复上传，提示用户
  - **发票有效性验证**：检测上传文件是否为有效发票，非发票文件提示"此不是发票文件请重新上传"
- 2026-01-26: 新增报销统计功能
  - 创建报销统计页面 ([src/views/ReimbursementStatistics.vue](../src/views/ReimbursementStatistics.vue))
  - 统计卡片展示：待审批、已审批、已驳回、总金额（包含数量和金额汇总）
  - 审批记录列表：显示所有报销单的审批状态和详情
  - 支持按报销类型、状态、日期范围筛选
  - 后端API接口：
    - `GET /api/reimbursement/statistics` - 获取统计数据
    - `GET /api/reimbursement/records` - 获取审批记录列表（支持分页和筛选）
  - 路由配置：`/reimbursement-statistics`
  - 功能特性：
    - 四个统计卡片：待审批、已审批、已驳回、总金额
    - 每个卡片显示数量和金额
    - 审批记录表格显示：单号、报销类型、报销事由、申请人、报销金额、状态、提交时间、审批时间、审批人
    - 支持查看详情跳转到对应报销单
    - 数据刷新功能
- 2026-01-26: **完整实现报销管理数据库和API功能**
  - **数据库设计**：
    - 新增 `reimbursements` 表：存储报销单主记录（ID、类型、标题、总金额、状态、描述、商务类型、客户、用户ID、申请人、提交时间、审批时间、审批人等）
    - 新增 `reimbursement_invoices` 表：存储发票明细（ID、报销单ID、金额、开票日期、发票号码、文件路径、销售方、购买方、税额、发票代码等）
    - 支持级联删除：删除报销单时自动删除关联的发票明细
  - **后端API实现** ([server/routes/reimbursement.ts](../server/routes/reimbursement.ts))：
    - `POST /api/reimbursement/create` - 创建报销单（支持多张发票）
    - `GET /api/reimbursement/list` - 获取报销单列表（支持按类型、状态筛选和分页）
    - `DELETE /api/reimbursement/:id` - 删除报销单
    - `GET /api/reimbursement/statistics` - 获取统计数据（从数据库实时查询）
    - `GET /api/reimbursement/records` - 获取审批记录列表（支持按类型、状态、日期范围筛选）
  - **前端功能对接**：
    - [src/views/BasicReimbursementDetail.vue](../src/views/BasicReimbursementDetail.vue)：提交报销单时调用 `/api/reimbursement/create` 保存到数据库
    - [src/views/BasicReimbursement.vue](../src/views/BasicReimbursement.vue)：页面加载时从数据库获取报销单列表，支持删除操作
    - [src/views/ReimbursementStatistics.vue](../src/views/ReimbursementStatistics.vue)：从数据库实时获取统计数据和审批记录
  - **功能特性**：
    - 报销单自动生成唯��ID（格式：R+日期+随机字符）
    - 支持基础报销、大额报销、商务报销三种类型
    - 报销单状态：draft（草稿）、pending（待审批）、pending_first（待初审）、pending_second（待复审）、pending_final（待终审）、approved（已通过）、rejected（已驳回）
    - 多张发票支持：一个报销单可包含多张发票，自动计算总金额
    - 数据持久化：所有报销数据存储在SQLite数据库中
    - 权限控制：用户只能查看和操作自己的报销单
- 2026-01-26: **实现保存草稿功能**
  - 修改后端 `/api/reimbursement/create` 接口，支持 `status` 参数（draft 或 pending）
  - 前端 `handleSaveDraft` 函数调用API并传入 `status='draft'`
  - 前端 `handleSubmit` 函数调用API并传入 `status='pending'`
  - 后端列表API返回数据时，将 `type` 字段转换为中文的 `category` 字段
  - 草稿状态的报销单可以在报销列表页面中显示和编辑
- 2026-01-26: **修复报销单创建失败问题**
  - **问题原因**：前端传递的 `type` 字段是具体报销类型（如 office_equipment），但数据库 `type` 字段只接受 basic/large/business
  - **解决方案**：
    - 数据库添加 `category` 字段存储具体报销类型
    - 前端提交时传递 `type='basic'` 和 `category='office_equipment'`
    - 后端API接收并存储 `category` 字段
    - 列表查询时将 `category` 转换为中文显示
  - 报销单标题格式改为：`YYYY年MM月报销-具体类型`（如：2026年01月报销-办公设备）
- 2026-01-26: **实现查看/编辑模式区分功能**
  - **功能需求**：
    - "查看"按钮：跳转到申请页面，但不具备保存和提交功能（只读模式）
    - "修改"按钮：跳转到申请页面，具备保存和提交功能（编辑模式）
  - **后端API新增**：
    - `GET /api/reimbursement/:id` - 获取报销单详情（包含发票明细）
  - **前端实现** ([src/views/BasicReimbursement.vue](../src/views/BasicReimbursement.vue))：
    - "查看"按钮：`router.push(\`/basic-reimbursement/\${row.id}?mode=view\`)`
    - "修改"按钮：`router.push(\`/basic-reimbursement/\${row.id}?mode=edit\`)`
  - **前端实现** ([src/views/BasicReimbursementDetail.vue](../src/views/BasicReimbursementDetail.vue))：
    - 新增 `pageMode` 计算属性：根据路由参数判断当前模式（create/view/edit）
    - 页面标题根据模式显示：新建报销单/查看报销单/编辑报销单
    - 查看模式下禁用所有表单输入和上传功能
    - 查看模式下隐藏保存草稿和提交审批按钮
    - 查看模式下隐藏发票删除按钮
    - 新增 `loadReimbursementDetail` 函数：从后端加载报销单详情数据
    - 组件挂载时自动加载数据（如果有ID参数）
  - **功能特性**：
    - 三种页面模式：create（新建）、view（查看）、edit（编辑）
    - 查看模式完全只读，无法修改任何内容
    - 编辑模式可以修改并重新提交
    - 自动加载现有报销单数据并显示
- 2026-01-26: **实现发票有效性验证功能**
  - 后端 OCR 识别后自动验证是否为有效发票
  - 有效发票判断条件：
    - 金额大于0
    - 有发票号码或发票代码
    - 有开票日期
    - 有销售方或购买方信息
  - 非有效发票返回错误：`此不是发票文件请重新上传`
  - 前端自动移除无效发票文件并显示错误提示
- 2026-01-26: **实现管理员审批功能**
  - **后端API新增** ([server/routes/reimbursement.ts](../server/routes/reimbursement.ts))：
    - `GET /api/reimbursement/pending-list` - 获取待审批报销单列表（仅管理员可访问）
    - `POST /api/reimbursement/:id/approve` - 审批/驳回报销单（仅管理员可操作）
  - **前端实现** ([src/views/ReimbursementStatistics.vue](../src/views/ReimbursementStatistics.vue))：
    - 在审批详情弹窗中添加"通过"和"驳回"按钮
    - 仅待审批状态的报销单显示审批按钮
    - 驳回时需要输入驳回原因
    - 审批操作需要二次确认
    - 审批后自动刷新统计数据和列表
  - **权限控制**：
    - 只有 super_admin 和 admin 角色可以进行审批操作
    - 普通用户无法访问审批接口
  - **审批流程**：
    - 管理员在"报销统计"页面查看待审批报销单
    - 点击"查看详情"打开审批弹窗
    - 审批通过：直接点击"通过"按钮并确认
    - 审批驳回：点击"驳回"按钮，输入驳回原因后确认
    - 审批结果记录审批时间、审批人和驳回原因（如有）
  - **功能特性**：
    - 支持批量查看待审批报销单
    - 审批操作实时更新报销单状态
    - 记录完整的审批历史信息
    - 驳回原因最多200字符
- 2026-01-27: **模块化重构**
  - 将报销详情页面拆分为独立的组件和逻辑层
  - **新增组件**：
    - `InvoiceUploader.vue` - 发票上传组件
    - `InvoiceTable.vue` - 发票明细表格组件
    - `TypeSelector.vue` - 报销类型选择器组件
  - **新增 Composables**：
    - `useInvoice.ts` - 发票操作逻辑（上传、删除、OCR识别）
    - `useInvoiceValidation.ts` - 发票校验逻辑（日期、重复、金额）
    - `useReimbursement.ts` - 报销单CRUD逻辑
  - **新增常量文件**：
    - `constants.ts` - 报销类型、状态等常量定义
  - **重构页面**：
    - `BasicReimbursementDetail.vue` 从856行减少到260行（减少70%）
    - `LargeReimbursementDetail.vue` 从912行减少到252行（减少72%）
  - **分区开发优势**：
    - 修改某一功能不会影响其他区域
    - 代码复用：两种报销类型共用同一套组件
    - 便于测试：可以单独测试每个 composable
    - 清晰的职责划分：组件只管展示，composable 管逻辑


- 2026-03-05: **优化发票日期校验规则**
  - 允许上传当年的所有发票（1月到12月）
  - 当年1月份可以上传去年12月的发票
  - 当年1月份不能上传去年11月及之前的发票
  - 其他月份不能上传跨年发票

- 2026-03-05: **优化发票上传功能**
  - 支持批量上传：一次可选择多个PDF文件
  - 支持拖拽上传：可将文件拖拽到上传区域
  - 优化上传提示：显示每个文件的识别进度和结果
  - 改进上传区域UI：更大的拖拽区域，更清晰的提示文字

- 2026-03-05: **新增核减金额功能**
  - 发票表格新增"报销类型"列，显示每张发票的报销类型
  - 发票表格新增"核减金额"列（可选显示，通过 `showDeduction` prop 控制）
  - 发票表格新增"实际报销"列（可选显示），自动计算：实际报销 = 发票金额 - 核减金额
  - 总计行显示：发票总金额、总核减金额、实际报销总额
  - 优化金额计算精度：使用分（整数）进行计算，避免浮点数精度问题
  - 核减金额大于0时以红色文字显示，为0或空时显示"-"
  - 实际报销金额以加粗样式显示，便于识别
  - 列宽优化：序号50px、报销类型100px、金额列100px、开票日期130px

- 2026-03-06: **新增无票上传功能（支付截图识别）**
  - **功能概述**：
    - 在基础报销页面新增"无票上传"功能，用于上传支付截图（微信/支付宝支付凭证）
    - 支持图片格式：JPG、PNG、GIF、BMP、WEBP
    - 使用 Tesseract.js 进行图片 OCR 识别
    - 自动识别支付截图中的关键信息并填充到报销表单
  - **识别字段映射**：
    - 商品名称 → 报销类型（如："招牌小面三件套"）
    - 支付金额 → 报销金额（如：34.50）
    - 支付时间 → 开票日期（如：2026年2月24日）
    - 交易单号 → 发票号码（如：4500000143202602247407359650）
  - **新增文件**：
    - `server/services/receiptOcr.ts` - 支付截图 OCR 识别服务
    - `src/components/reimbursement/ReceiptUploader.vue` - 收据上传组件
  - **修改文件**：
    - `server/routes/reimbursement.ts` - 新增 `POST /api/reimbursement/upload-receipt` 接口
    - `src/composables/reimbursement/useInvoice.ts` - 新增 `handleReceiptChange` 方法
    - `src/components/reimbursement/InvoiceTable.vue` - 支持图片缩略图预览
    - `src/views/BasicReimbursementCreate.vue` - 集成无票上传功能
  - **技术实现**：
    - 使用 Tesseract.js 进行图片文字识别（支持中英文）
    - 正则表达式解析支付截图文本，提取关键信息
    - 图片文件存储在 `uploads/invoices/` 目录，文件名前缀为 `receipt-`
    - 发票表格自动识别图片文件，显示图片缩略图而非 PDF 图标
    - 点击缩略图可预览完整的支付截图
  - **用户体验**：
    - 无票上传区域与发票上传区域样式一致，主题色为绿色
    - 支持拖拽上传图片文件
    - 上传后自动进行 OCR 识别并显示进度
    - 识别成功后自动填充报销信息到发票明细表格
    - 支持删除已上传的支付截图

- 2026-03-07: **优化支付截图识别功能**
  - **报销类型默认值**：
    - 所有支付截图识别后，报销类型统一填充为"无票报销"
    - 移除商品名称识别逻辑，提升识别速度
  - **新增预验证机制**：
    - 在执行完整 OCR 之前，先快速验证图片是否为支付截图
    - 验证条件：
      1. 必须包含支付成功相关关键词之一（支付成功、转账成功、对方已收钱、收款成功、付款成功、交易成功，6 个关键词满足其一即可）
      2. 金额特征为可选项（如果预验证阶段未识别到金额，仍可通过，金额会在完整 OCR 中识别）
    - 验证失败时统一提示："此不是支付截图，请重新上传"
  - **新增交易单号查重功能**：
    - **前端本地查重**：在同一报销单中，检查当前发票列表是否已有相同交易单号
    - **后端数据库查重**：OCR 识别完成后，检查数据库中是否已被其他人使用
    - 查重逻辑：通过 OCR 识别的交易单号判断是否为"一样的"图片
    - 如果发现本地重复，提示："发票号码 [交易单号] 已存在，请勿重复上传"
    - 如果发现数据库重复，提示："[申请人姓名] 已上传此支付截图，请勿重复上传"
    - 查重失败时自动清理临时文件，阻止重复上传
  - **技术实现**：
    - 新增 `validatePaymentScreenshot()` 函数进行预验证
    - 使用缩小图片（800x1400）加快验证速度
    - 验证通过后才执行完整的 OCR 识别流程
    - 当所有 OCR 策略都未识别到金额时，抛出错误而非返回空结果
    - 在 `POST /api/reimbursement/upload-receipt` 路由中添加查重逻辑
    - 通过 JOIN 查询关联 `reimbursements` 表获取申请人信息
    - 所有错误统一提示"此不是支付截图，请重新上传"
    - 错误信息通过 API 返回给前端，用户可看到具体失败原因

- 2026-03-11: **优化管理员审批中心，区分商务报销审批和付款职责**
  - **功能调整**：
    - 商务报销（type='business'）由总经理审批，管理员不参与审批流程
    - 管理员只负责商务报销的付款操作
    - 审批完成后抄送管理员进行付款处理
  - **后端修改** ([server/routes/approval.ts](../server/routes/approval.ts))：
    - 待审批列表查询：排除商务报销（`r.type != 'business'`）
    - 待审批统计数量：排除商务报销
    - 本月已通过统计：排除商务报销
    - 本月已通过列表：排除商务报销
  - **影响范围**：
    - 管理员审批中心不再显示商务报销的待审批项
    - 统计数据中不包含商务报销的审批数量
    - 管理员仍可在"已通过未付款"和"已付款"列表中看到商务报销，进行付款操作

- 2026-03-09: 报销列表删除按钮支持所有状态（不再限制仅草稿可删除）；报销列表默认只显示当月数据（从当月1号开始）；新增月末自动清除定时任务，每月最后一天23:55自动软删除历史报销数据，历史数据在报销统计中仍可查看

- 2026-03-11: **优化管理员审批中心导出功能**
  - **全部查询新增报销范围筛选**：
    - 在全部查询筛选区添加"所属区域"级联选择器
    - 支持按报销范围/区域进行多选筛选
    - 基础报销不支持报销范围筛选（自动禁用）
  - **导出数据按报销范围分类统计**：
    - 导出预览弹窗新增"报销范围汇总"表格
    - 显示每个报销范围的报销笔数和金额
    - 仅统计大额报销和商务报销的报销范围数据
  - **导出Excel格式优化**：
    - CSV文件新增"报销范围汇总"部分
    - 按报销范围展示分类汇总数据
    - 便于管理员清晰查看各区域的报销情况
  - **同步报销范围管理**：
    - 管理员在报销管理中新增的区域自动同步到审批中心
    - 通过 `/api/reimbursement-scope/list` 接口获取最新的报销范围列表

- 2026-03-11: **优化全部查询金额显示和区域筛选**
  - **金额只显示已付款的实际银行转账金额**：
    - 全部查询中的金额字段只对已付款状态（paying、payment_uploaded、completed）显示实际转账金额
    - 未付款的报销单（pending、approved、rejected等）金额显示为"-"
    - 汇总卡片和总计金额只统计已付款金额，与银行流水对应
    - 导出数据同步只统计已付款金额
  - **所属区域级联选择器支持父级包含子级**：
    - 选择一级区域时，查询自动包含该区域下所有子级区域
    - 通过前端 `expandScopeSelection` 函数递归展开父级为所有子孙 value

- 2026-03-11: **新增发票管理功能**
  - **功能概述**：
    - 在管理员审批中心"全部查询"标签页后新增"发票管理"标签页
    - 支持按员工、报销类型、所属区域、日期（月/日）筛选发票
    - 支持批量下载发票（ZIP打包）
    - 支持批量在线打印（发票和收据/无票），无需下载
  - **后端API新增** ([server/routes/approval.ts](../server/routes/approval.ts))：
    - `GET /api/approval/invoice-management` - 查询发票列表（支持多维度筛选）
    - `POST /api/approval/invoice-management/batch-download` - 批量下载发票（ZIP打包）
  - **前端实现** ([src/views/ApprovalCenter.vue](../src/views/ApprovalCenter.vue))：
    - 新增"发票管理"标签页，包含筛选区、操作区、发票列表
    - 筛选条件：员工、类型（多选）、所属区域（级联多选）、日期（月/日切换）
    - 发票列表显示：员工、报销类型、文件类型（发票/收据）、金额、发票日期、发票号码、报销事由、预览
    - 支持全选/单选发票，显示已选数量
    - 批量下载：选中发票后打包为ZIP下载，文件名包含员工姓名和发票信息
    - 批量打印：选中发票后打开打印预览窗口，每张发票独立一页，支持浏览器在线打印
    - 发票预览：点击"查看"可预览发票PDF或收据图片
  - **新增依赖**：
    - `archiver` - 用于服务端ZIP打包
    - `@types/archiver` - archiver类型定义
- 2026-03-12: 安全修复 - 修复报销详情页（基础/大额/商务）TypeScript 类型错误和 ESLint error，包括 useInvoice 调用签名修正、handleExceed 方法替换、未使用变量清理、InvoiceTable 模板属性修正
- 2026-03-13: 修复 InvoiceUploader/ReceiptUploader 查看报销单时发票文件不显示的问题，将双 watch 同步模式改为 computed ref 直接桥接
- 2026-03-13: 调整报销列表显示顺序 — 基础/大额/商务报销页面按 草稿→待审批→已驳回→待付款→待确认→已完成 排序；报销统计页面按 待审批→已驳回→待付款→待确认→已完成 排序，删除"付款中"筛选选项
- 2026-03-13: 修复报销详情页查看时发票明细为空的问题 — loadInvoices 方法增加 filePath 等字段的空值保护，防止 null 值导致运行时崩溃；三个详情页增加 invoices 数组类型校验；后端和前端增加调试日志辅助排查
- 2026-03-13: 修复基础报销详情页无票附件不显示问题 — 基础报销详情页补充 ReceiptUploader（无票上传）展示区域，并在详情加载时按文件类型拆分 PDF 发票与图片收据，确保已完成报销查看时顶部附件区域与下方发票明细保持一致
- 2026-03-13: 实现菜单栏待办事项实时提示功能
  - 基础/大额/商务报销菜单项显示待确认收款数量（黄色角标）
  - 财务区分组显示待办小红点（包含用户报销待确认、管理员审批待办、总经理审批待办）
  - 确认收款操作后自动刷新待办计数
  - 集成 pending store 实现实时更新
- 2026-03-13: 审批流程显示历史驳回记录
  - **后端修改**：
    - 修改 `/api/reimbursement/:id` 接口，查询审批历史时包含所有历史审批实例的记录
    - 修改 `/api/approval/by-target` 接口，返回所有历史审批记录而非仅当前实例
  - **前端修改**：
    - 基础报销页面 ([src/views/BasicReimbursement.vue](../src/views/BasicReimbursement.vue)) 已有审批历史显示逻辑
    - 大额报销页面 ([src/views/LargeReimbursement.vue](../src/views/LargeReimbursement.vue)) 新增审批历史显示
    - 商务报销页面 ([src/views/BusinessReimbursement.vue](../src/views/BusinessReimbursement.vue)) 新增审批历史显示
    - 审批中心和总经理审批中心通过修改后的接口自动获取历史记录
  - **效果**：报销单被驳回后重新提交，审批流程中会显示之前的驳回记录和驳回原因


