# 团队日志图片鉴权预览生产发布

2026-09-22 已完成生产发布。生产固定运行镜像 `yulilog-prod-yulilog:team-image-preview-20260922-v3`，镜像摘要为 `sha256:1c3bdba5a95c3c76fdc9023b4b0a83ccbb2dd41fba706d4e2546ffe4765ef0e2`；应用与 Nginx（反向代理）健康，重启次数为零。

## 问题与修复

2026-09-21 的图片附件已经成功上传：数据库记录为 `image/png`、44,010 字节，生产挂载文件存在且文件头为有效 PNG（便携式网络图形）。原团队日志页面把物理 `filePath`（文件路径）直接拼成 `/uploads/...`；生产没有公开上传目录，该请求被前端单页回退捕获，返回 `200 text/html`，图片组件因此无法解码。

本次只修改 `src/views/TeamDailyLogs.vue`：缩略图和大图预览统一使用现有登录鉴权接口 `/api/daily-logs/team/attachments/{附件编号}/preview`。文档预览、数据库、附件记录、物理文件和服务端均未修改。

## 验证

- 源码范围仅变更团队日志页面；205 个活动前端资源中，202 个逐字一致或仅有引用／作用域变化，真实逻辑变化限定为前端入口和团队日志脚本。
- 候选镜像相对上一生产镜像的服务端文件差异为零。
- 专项测试验证缩略图和大图均使用附件编号鉴权地址，不再引用 `/uploads`。
- 完整 TypeScript（类型脚本）检查及 297 个测试套件、3967 项测试全部通过。
- 候选与正式生产均使用真实生产图片进行只读验收：返回 `HTTP（超文本传输协议） 200`、`Content-Type（内容类型） image/png`、44,010 字节，SHA-256（安全散列算法）摘要为 `97f53634a4373b41cd078dd4023b36f364d8734b05ffa313e9ea4f4e4b05688d`，与物理原文件完全一致。
- 本次不包含数据库迁移，不新增、更新或删除任何附件数据。

## 发布与回滚

受控发布资料位于 `backup/team-image-preview-release-20260922.v3/`。候选以 `team-worklog-20260921-v2` 为基础镜像，仅覆盖隔离构建的完整前端产物。`rollback.yml` 固定指向上一生产镜像，应用回滚不会改动数据库和附件。

## 更新日志

- 2026-09-22: 团队日志图片缩略图和大图预览改用登录鉴权接口，真实生产 PNG（便携式网络图形）逐字节验收通过，正式上线且未修改附件数据。
