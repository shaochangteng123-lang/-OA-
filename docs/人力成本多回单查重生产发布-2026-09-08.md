# 人力成本多回单查重生产发布

2026-09-08 11:39（北京时间）完成生产上线验收。

## 已上线功能

- 社保、公积金支持多张选择与已有回单后继续追加，按月份和类别累计有效识别金额。
- 根据电子回单号跨文件、跨月份、跨社保与公积金类别查重；同号文件不重复入账，缺失电子回单号的笔不放行。重复文件失败时保留明确原因，不暂停后续任务。
- 新增人力成本回单号登记表，号码唯一主键与事务锁防止并发重复；删除原件记录时级联释放号码，重识别排除自身。
- 社保兼容有完整国库用途及交易证据、大小写金额一致的工行遮挡账号回单，沿用用户选择的费用类别。
- 社保识别版本为 5，公积金为 4；加载月份时为旧版回单排队重识别并建立查重登记，未经查重的旧版金额暂不计入汇总。

## 验证结果

- 246 套、3,391 项完整测试通过；前后端类型检查、前后端构建通过。
- 生产备份恢复到独立网络的临时数据库，候选应用完整启动迁移成功；原业务数据摘要保持一致。
- 隔离环境真实上传接口验证：先上传 40.17 元，再追加 302.41 元，累计为 342.58 元。
- 在隔离数据库调整原文件摘要以模拟不同截图后，同电子回单号重新上传被拒绝，累计仍为 342.58 元；跨月份重复被拒绝；删除原件和失败上传记录后重新上传成功。
- 正式生产容器原图完整识别成功：40.17 元，电子回单号 0920-2021-0505-1100，完整收款账号为空，未伪造遮挡数字。
- 正式容器健康、反向代理配置校验和重载、首页与人事页面资源逐字节核对通过。
- 生产切换前后财务业务表以及人力成本回单、回单明细的数据摘要一致。没有向正式数据库写入测试账号或测试回单。

## 发布范围与回滚

以既有个税修复镜像为基础，仅替换工资路由、人力成本识别、数据库初始化文件并新增回单号登记服务。前端发布人事页面所需资源，保留现行月度财务页面资源与全部旧资源，未带入本地月度财务页面差异。

- 当前镜像：`yulilog-prod-yulilog:human-cost-receipts-20260908`。
- 回滚镜像：`yulilog-prod-yulilog:tax-receipt-20260908`。
- 发布资料与切换前数据库备份：`backup/human-cost-receipts-release-20260908/`。
- 重新启动当前版本使用该目录 `release.yml`（发布配置）结合仓库生产编排文件；回滚应用使用 `rollback.yml`（回滚配置），之后重载反向代理。新增表可保留，不通过覆盖数据库回滚应用。
- 本轮隔离测试应用、数据库及其临时卷和网络完成验收后清理；数据库备份、测试日志、镜像及回滚配置保留。

## 更新日志

- 2026-09-08: 社保、公积金多张追加、电子回单号查重及社保遮挡账号识别正式上线；生产原图、页面资源、服务健康与数据摘要验收通过。
