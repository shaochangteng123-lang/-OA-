# 月度财务趋势生产上线记录

## 发布结果

2026-09-04 12:04（上海时间）已切换正式生产应用并重载反向代理。生产应用与数据库健康，生产80端口的健康检查及页面均返回成功；未登录访问分析接口返回401，权限保护正常。

发布镜像：`yulilog-prod-yulilog:finance-20260904-kidqza-safe`。

镜像摘要：`sha256:e8e1f0cf04357e597a4c1d976c524edda398121087c0c3176c16e4dca6e24b8d`。

开发环境未切换，生产数据库卷、附件挂载和现用配置未改变，没有导入开发数据。

## 发布范围和启动保护

- 与实际线上产物比较，本批变化限财务趋势、项目回单预览、税费分类及其前端资源。其余合同、工资、识别等代码此前已在线上，不把未提交状态误判为未上线。
- 旧启动迁移会重写一条非空的发票方向关联，已按用户确认修复：仅在关联缺失、源发票存在且方向兼容时补齐，已有选择不覆盖。
- 四账户字典内容相同时不再更新时间；真正变化时仍按原规则更新。
- 税费分类、账户／方向及说明／凭证号约束已在生产迁移并验证。没有新增或回填财务金额。

## 验证证据

- 完整回归：208套、2,968项全部通过，前后端类型检查通过。
- 新增18项启动保护回归通过；候选镜像中实际数据库语句的22项断言通过，临时数据已回滚。
- 生产备份副本演练：49张财务相关表、2,646行，在完整启动迁移前后、查询及导出前后的内容摘要完全一致。
- 月度、季度、年度及上年同期均返回七模块，金额保持精确文本／真实未知；全部模块导出成功。
- 正式生产切换前后，对合同、回款、财务登记、账户、月报、手工项目、月结快照、审计和工资共九张关键表逐行汇总散列，结果完全一致；原有发票关联未被重写。
- 实际生产主包 `/assets/index-DcCMQfFH.js` 与月报包 `MonthlyFinancialReport-DKXd3npA.js` 可正常访问；月报包含“回到本月”、图表按钮类、银行回单预览和合同编号新功能。

## 备份与回滚

本次私有备份目录：`backup/monthly-financial-release-20260904.KiDQza`，目录权限700。

- 最新数据库备份：`production-protected-before.dump`；安全散列摘要 `3a7018bfb294207f92b2c45cbbf91f18508013f022fbcd837c7c764b1f3c8505`。
- 最新附件备份：`uploads-production-protected-before.tar.gz`；安全散列摘要 `c1545ba6a387f7f6a6d886af842d90be07d1a247f842207606cae55cff8a3bd5`。
- 原生产镜像保留为 `yulilog-prod-yulilog:finance-rollback-20260904`，原镜像摘要 `sha256:cbcea281d96532ffcdccf844b1c19de39eac60576edc06cf9008a4f4dec5fbae`。
- 本次固定版本配置为目录内 `compose.release.yml`；回滚配置为 `compose.rollback.yml`。生产基础配置、网络及持久化目录继续沿用原文件。

旧版本既不识别新增税费分类，也没有本次启动覆盖保护。因此回滚前必须核对新增税费记录和已有发票关联，优先制作保留启动保护的兼容回滚镜像；不能直接盲目启动旧镜像或覆盖生产数据库。

全量结果：`/tmp/financial-production-protected-full-20260904.json`。隔离演练、真实语句检查及切换前后指纹均保存在本次备份目录。

## 界面核验状态

用户登录后已完成一轮生产真实数据只读交互检查：

- 七模块均可切换，每块主折线和图内“回到本月”入口存在，未见加载错误。
- 八月账户点选与当月饼图一致，四账户精确合计为 `¥1,860,914.27`；一般账户八月支出点选与饼图圆心均为 `¥162,771.08`，缺少税费凭证的分类保持未知。
- 七月本月回款 `¥209,000.00` 只列两个正回款项目，按行政区分组并展示本月回款／合同金额／已回款／未回款四卡；本月回款独立底色。
- 真实银行回单在受权临时地址下加载并显示，关闭后弹窗消失；全屏内预览可用，退出键只关闭回单，不退出分析全屏。
- 累计已回款点选显示57个项目的合同编号与三项金额，没有银行回单入口或内部来源编号行。
- 实际零值显示 `¥0.00` 且相邻线段连续；年度对比开关可关闭／恢复。月度12月、季度12季及年度按所选真实年份分组均生效。
- 全屏使用当前浏览器支持的页面铺满模式，七个模块在全屏内切换不会退出；真实滚轮浏览历史时保留12刻度，不改变上方统计范围。“回到本月”可恢复截至本月的12月并查询。
- 未观察到浏览器控制台错误，验收后恢复原 `2026-01` 至 `2026-09` 月度、上年同期、账户余额模块视图。

生产数据本身的缺口已确认：当前只有2026-08保存了月报，2026-01至07、09及所查上年月份没有月报，所以账户余额／结算保留未知；另有缺少付款业务日期的历史报销，不按提交日期猜测归月。这不属于未上线或用零填充的情况。

本轮曾发现历史缓存同期说明使用整个缓冲范围的问题。用户确认后，已于13:45仅更新前端显示标签并补发生产；真实滚轮及五年季度点选核验通过，金额与实际期间映射保持。详见 `docs/月度财务同期日期说明补发记录-2026-09-04.md`。前述已核验交互中的该项问题现已闭环，生产数据缺口仍按真实情况显示未知。

## 更新日志

- 2026-09-04: 同期说明问题已修正并补发生产，真实月度滑动与五年季度点选通过，关键财务记录保持不变。
- 2026-09-04: 用户登录后完成上述主要交互验收，确认生产数据缺口，发现缓存同期图例文案范围不准确，待确认修正；本轮未修改源码或财务记录。
- 2026-09-04: 财务趋势与启动迁移保护正式上线，服务与数据不变检查通过。
