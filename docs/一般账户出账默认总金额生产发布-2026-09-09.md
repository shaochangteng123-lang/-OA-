# 一般账户出账默认总金额生产发布

## 发布结果

2026-09-09已发布生产。一般账户出账统计首次进入默认选择“总金额”；点击月度、季度或年度点位后，饼图展示同期间行政、薪资、一般账户跨行手续费、实际税费、资产类合同五类明细，圆心与折线使用同一精确分类总金额。银行实际支出继续保留在上方摘要独立核对。

## 发布范围

- 原生产镜像：`yulilog-prod-yulilog:hcr-layout-fix-latest-20260908`，镜像编号`sha256:535817ba375b67cc8eb645944ef4357d004b83958c9d8129dea8733724369b2a`。
- 新生产镜像：`yulilog-prod-yulilog:outflow-total-20260909`，镜像编号`sha256:0c1e8d0bf3633584f7b40c7758efb722e472c4d0b855a5db9c573a34f8fba4b7`。
- 仅变更`server/services/monthlyFinancialAnalysis.js`（财务分析服务）和活动页面`assets/MonthlyFinancialReport-BNx-AtZ1.js`（月度财务页面脚本）。镜像逐文件核对1,657个文件，除上述2个文件外1,655个文件与原生产完全一致。
- 主入口、页面样式、上传目录、数据库卷、原银行总额序列、其他六个分析模块和所有其他生产页面保持不变。

## 精确金额验收

- 2026年1月至9月已知分类总金额：906,335.52元。
- 2026年8月：行政10,331.05元、薪资70,683.97元、跨行手续费9.00元、实际税费未知、资产80,747.06元，圆心与折线均为161,771.08元。
- 2026年第三季度：338,555.17元，保留8月已确认跨行手续费9.00元；未知月份和未知税费没有按零补齐。
- 年度导出共5张工作表、34,907字节，同时保留分类总金额906,335.52元和银行实际总支出两个独立口径。

## 发布验证

- 7套142项专项测试通过；代码规范、格式和前端生产构建通过。
- 用发布时刻正式库备份启动隔离环境；启动迁移、月／季／年、本期／同期、其他六模块、导出和最终压缩页面交互全部通过，隔离业务数据读取前后完全一致。
- 最终页面默认“总金额”；月度和季度点位显示“已知部分”，真实零显示`¥0.00`并保持连线。点击8月点位后五类金额及占比与后端逐分一致；分类仍有未知来源时不误称“完整分类占比”。
- 生产健康接口返回200，活动页面脚本摘要为`ac15bedeafe7b146fb52bf4353f3a60812ff08c093f90420d65df48774932c89`。`ETag`（资源校验标识）已变化，携带旧标识请求返回200并取得新内容。
- 切换前、切换后及生产资源生效后，55张相关业务表内容摘要均为`8bf5a25cb124c52c3186b59487b0e8cf221bbbf3f930d97650f56768d3f23fe5`；本次没有写入或修正业务数据。

## 备份与回滚

发布材料位于`backup/outflow-total-release-20260909.B809bq/`。其中保留生产基线、最终候选、镜像逐文件清单、正式库切换前备份、接口响应、导出文件、浏览器验收和生产资源校验记录。回滚配置为`rollback.yml`，原镜像仍保留；如需回滚，只切换应用镜像，不以数据库备份覆盖上线后的正常业务数据。

应用容器重建会使原内存登录会话失效，用户需重新登录；这不影响业务数据或上传文件。

## 更新日志

- 2026-09-09: 一般账户出账默认总金额及点位五类饼图正式上线；最小双文件发布、精确金额、缓存更新和数据不变校验通过。
