# 请假抄送提醒上线与当日补发

2026-09-20 已完成生产上线、当日定向补发及正式终验。生产新镜像健康，两名接收人的提醒均保持未读，留待本人登录查看；原请假内容、审批状态及既有业务数据未改动。

## 发布范围

- 管理员和超级管理员收到本人请假抄送的站内提示、菜单及页签未读角标，点击提示进入“抄送记录”，支持只看未读。
- 成功查看详情后，只清除当前接收人实际看到的通知版本；其他接收人的已读状态互不影响。本人申请不自发提醒，本人审批事件自动已读，草稿和旧申请版本不计入未读。
- 原请假内容、审批流程、审批结果、余额与附件不因本次功能上线而改写；不全量回填历史提醒。

旧镜像为 `yulilog-prod-yulilog:hr-superadmin-20260919-xdaxge`，当前生产镜像为 `yulilog-prod-yulilog:leave-cc-20260920-1uutxn`。发布资料保存在 `backup/leave-cc-release-20260920.1uuTxN/`。

当前生产镜像编号为 `sha256:4d2f322180c79599ce43973538bca5b3ea25987befce054265e48cf2c8aa0940`。

前端从上次已上线源码隔离构建，仅覆盖以下五个文件，未携带工作区其他改动或新依赖：

- `src/stores/pending.ts`
- `src/utils/leaveApi.ts`
- `src/layouts/MainLayout.vue`
- `src/views/EmployeeData.vue`
- `src/components/leave/LeaveAdminPanel.vue`

服务端仅替换 `routes/leave.js`、`routes/approval.js`，新增 `services/leaveCcNotice.js`；数据库初始化沿用生产基线，只追加 `leave_cc_notifications`（请假抄送通知表）及其索引，不携带工作区其他历史迁移。

## 当日定向补发

用户要求新功能同时覆盖今天已有的请假。只针对已核对的赵双申请 `QJ-2026-00005`，该申请为已批准带薪事假，请假区间为 2026-09-28 至 2026-09-30。

已向管理员吴静雯和超级管理员账号 `admin` 补发，共新增两条未读通知。补发只向新增通知表追加记录，未重新提交或审批申请，未修改原申请、审批日志、余额和附件。重复执行不重复新增，也不覆盖已经查看后的已读状态。正式验收后两条提醒仍未读，未替接收人执行已读确认。

## 验证情况

- 已通过前后端类型检查及完整 291 套、3,927 项测试。
- 前端构建通过，构建前后源码白名单审计均为指定五个文件；205 个活动资源中 200 个无实际结构变化，187 个公共文件逐字节一致。
- 隔离副本完整启动并创建空通知表后，全部原有业务数据摘要不变；定向补发首次新增两条通知，通知接口和权限验收通过。
- 隔离副本实际确认一名接收人已读，另一人仍未读；重复补发新增零条，且未覆盖已读，原请假、日志与余额摘要不变。隔离写验收未操作生产数据库。

## 正式终验

- 新镜像运行健康，正式启动后新增通知表为空，全部原有业务数据保持不变。
- 定向补发实际新增两条通知；随后仅使用只读请求核验两名接收人的角标、未读列表及申请详情，全部通过，两人仍未读。
- 正式首页及九个关键脚本／样式资源与受控候选逐字节一致。
- 正式终验后，除新增通知表外，全部既有业务数据摘要仍为 `a5712ed4472dc0e375a38da52c8de1a779286a1289c902e6e3d7453c34b7b3d9`。未改请假审批状态、原日志、余额或附件。

## 备份与回滚原则

本轮完整备份 `production.dump`、`production-cutover.dump`、受控构建源码、产物、测试日志及验收结果保存在上述发布目录。`rollback.yml`（回滚配置）指向上一版人力资源权限镜像；应用回滚只切换镜像，不删除通知表，不覆盖上线后正常产生的业务数据。完整数据库备份仅用于隔离恢复验证或经明确授权的事故恢复。

终验后已精确删除生产容器内本次临时验收文件及空目录，并清理本次专用隔离容器、网络和数据卷；本地备份与验收材料保留，可重建隔离环境。生产应用保持健康，开发应用、正式数据库和反向代理持续运行，未受清理影响。

## 已有验收材料

- `release-check.log`：完整测试与类型检查。
- `frontend-source-scope-audit.json`、`frontend-bundle-structure-audit.json`：受控源码及前端产物范围。
- `isolated-before.txt`、`isolated-after-start.txt`：隔离启动前后业务摘要。
- `results/isolated-apply.json`、`results/isolated-verify.json`、`results/isolated-test.json`：补发、只读接口及实际已读隔离验收。
- `results/isolated-idempotency.json`：重复执行不重置通知、不修改原业务数据。
- `results/production-apply.json`、`results/production-verify.json`：正式补发和只读接口终验，两人均保持未读。
- `results/public-assets.json`：正式首页及九个关键资源逐字节核验。

## 更新日志

- 2026-09-20: 建立请假抄送提醒发布及当日定向补发记录；候选构建、源码范围审计和 3,927 项测试通过，隔离验收中，尚未确认正式上线。
- 2026-09-20: 隔离副本启动、两条定向补发、权限与已读隔离、重复补发不覆盖已读全部通过，原业务数据摘要保持不变；生产尚未切换。
- 2026-09-20: 正式上线及当日定向补发完成；两人各保留一条未读提示，正式角标、列表、详情和公开资源验收通过，除新增通知表外全部原业务数据摘要不变。
