# 福利分类开放月报实时联动生产发布

## 发布结果

2026-09-17已发布生产。管理员在报销管理中启用、停用、改名或排序福利1、福利2分类后，重新进入开放状态月报即可显示最新分类状态，不再要求点击“同步自动数据”。实时联动只覆盖分类名称、顺序和启停状态，已保存自动金额、手工明细和月报工作版本不变；已月结月份继续使用不可变快照。

## 发布范围

- 原生产镜像编号：`sha256:2a115dbffb8985653ef3b49d39503da3d61fc52d1c653be8295e79758dd89647`。
- 新生产镜像：`yulilog-prod-yulilog:welfare-live-catalog-20260917-htku1m`，镜像编号`sha256:61b43747ecd3a576d8641d333b59886d949af21dec53969cba98c30d2f71ca4e`。
- 生产镜像共617个文件，仅替换`server/services/monthlyFinancialReport.js`（月报服务）和`server/routes/monthly-financial-reports.js`（月报路由）；其余615个文件与原生产逐字节一致。
- 不新增接口、依赖、数据库字段或启动迁移，不修改前端资源。

## 验收结果

- 3套64项专项测试、服务端类型检查、规范检查和编译通过。
- 最新正式数据副本启动迁移前后完全一致；候选读取前后业务数据未变化。
- 2026年9月开放月报保持`draft（草稿）`、工作版本1：407-AI、8H-AI均为启用，页面显示“添加”，金额均为0.00元；其他金额、账户、手工明细、校验和权限与生产基线一致。
- 2026年8月保持`closed（已月结）`、工作版本与月结版本均为24：407-AI保留7笔12,103.00元，8H-AI保留1笔1,457.50元，页面只读且无新增入口；候选响应与原生产快照完全一致。
- 正式容器内只读接口复核通过：9月两项实时启用、金额和版本不变，8月月结版本不变。
- 切换前、切换后及正式接口复核后，排除登录会话表的126张业务表内容摘要均为`b3b0fe47e6538b18043a84be96de30a8b2ba3325e60ab79f9d41e6e6f2c209f4`。

首次切换门禁发现的唯一变化是`session（登录会话）`表的会话内容更新；排除该临时会话表后，全部业务表完全一致。系统随后使用最新正式备份重新完成隔离验收，再执行生产切换。

## 备份与回滚

发布材料位于`backup/welfare-live-catalog-release-20260917.htKu1M/`。切换前正式库备份大小为1,314,303字节，摘要为`a50ec80a5eb35b26acd740d453a855ab2f93bf227c8c0a915e68867256cf8954`。原镜像已保存为`yulilog-prod-yulilog:welfare-live-base-20260917-htku1m`，回滚配置保存在`rollback.yml`；如需回滚，只切换应用镜像，不用数据库备份覆盖上线后的正常业务数据。

应用容器重建会使原内存登录会话失效，用户重新登录后即可使用；业务数据和上传文件不受影响。

## 更新日志

- 2026-09-17: 福利分类与开放月报实时联动正式上线；9月新增入口、8月冻结快照、最小双文件发布和126张业务表不变校验通过。
