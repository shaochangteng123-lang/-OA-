# 个税遮挡账号回单生产发布

2026-09-08 11:07（北京时间）完成上线与生产原图验收。

## 发布范围

以现行生产镜像为基础，仅替换付款回单解析、人力成本回单识别、工资路由三个后端编译文件。已逐项比较生产旧文件与候选文件，差异仅包含本次修复。前端、数据库结构、依赖和其他服务沿用现行生产版本。

个税识别版本由 3 升至 4，打开对应月份时沿用既有队列自动重识别旧版本回单。只对有完整国库用途及交易证据、收款栏明确且大小写金额一致的工行遮挡账号税款回单兼容；完整账号保持为空，不推测被遮挡数字，不将备注长编号冒充账号。

## 验收

- 完整发布检查通过：244 套测试、3,378 项断言对应测试用例及前后端类型检查通过，后端编译通过。
- 候选镜像在无网络、无数据库连接条件下完成原图识别。
- 正式生产容器完整原图识别结果为已识别，金额 302.41 元，收款单位为待报解预算收入，收款账号为空，识别错误为空。
- 生产容器健康正常；反向代理配置校验、重载及宿主机生产首页和健康接口访问通过。
- 切换前后既有财务业务表数据摘要完全一致。未通过验证程序创建、修改回单业务记录；页面加载触发的版本升级重新识别仍按正常业务流程执行。
- 首次生产原图验收因临时图片复制后的文件归属导致读权限不足；仅修正该临时文件归属后重试通过，不涉及上传目录或业务附件权限变更。

## 备份和回滚

- 发布资料：`backup/tax-receipt-release-20260908/`，包含数据库备份、旧文件、候选文件、测试结果、生产识别结果、切换日志和数据摘要。
- 新镜像：`yulilog-prod-yulilog:tax-receipt-20260908`。
- 回滚镜像：`yulilog-prod-yulilog:outflow-metrics-20260908-juf4om`。
- 重启当前版本时，将资料目录的 `release.yml`（发布配置）与仓库 `docker-compose.prod.yml`（生产编排配置）共同使用，并执行反向代理重载脚本。回滚应用使用同目录 `rollback.yml`（回滚配置），不覆盖恢复生产数据库。

## 更新日志

- 2026-09-08: 个税遮挡账号精准识别修复已上线，生产原图识别 302.41 元，服务健康及切换数据摘要核验通过。
