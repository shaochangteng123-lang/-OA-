# 月度财务趋势开发与验收清单

目标：按桌面《月度财务趋势需求确认书-2026-09-02》实现七个业务模块，避免无关重构，完成多轮测试及实际页面验证后交付。

当前状态：自动验收完成。七模块、实际税费、五项界面整改及最终代码两轮全量回归已通过；最后的浏览器文件保存已通过隔离浏览器连续两轮、每轮六场景的实际下载落盘及独立回读复核，不再依赖用户手动确认。历史无有效税费记录的月份仍保持未知，不属于自动补录范围。以下保留各轮独立证据；按用户要求未部署生产。

## 本轮用户反馈五项整改检查

以下为独立的本轮验收项。代码和文档接入不等于视觉验收完成，须补浏览器画面与行为测试证据后再勾选。

1. [x] **七模块不要同时铺开**：导航切换一次只显示一块，默认账户余额；各块摘要、图表、来源、统计表及分页明细仍可访问，原历史图入口保留。
2. [x] **趋势首屏空间**：压缩维护区，维护操作不占趋势首屏，校验／来源／长提示按需折叠；1440 像素宽屏检查通过，390 像素窄屏页面宽度仍为 390，未向外溢出。其他页签维护入口继续可用。
3. [x] **默认往年对比及指标切换**：默认上年同期，可选历史年或不比较；本期实线与灰紫对比虚线按同指标对齐，支持单指标切换和显式多指标；跨年、缺期、双期工资权限及 29／5 表导出检查通过，无比较保持 15／3 表。
4. [x] **精准金额直接可读**：双期有效点含零值均有完整金额标签，至少两位小数、更多小数不截断。真实项目图 18 标签无碰撞；合成双期同值和超长金额 22 标签无碰撞；主动四条线 44 标签无碰撞。标签有底衬及引导线，不移动真实点坐标。
5. [x] **结构图不再无说明空白**：真实账户、商务和所选人员结构图均已显示。只用已知正金额计算比例，负数／未知／零值另列，明确不代表净额或完整总额；余额回看时显示具体日期及非所选期末，不替代摘要。无数据、未选人员或无工资权限保留中性空态与原因。

### 本轮界面整改的验收证据

- 2026-09-02 22:03（北京时间）对最终代码连续运行 `visual-final-1.json`、`visual-final-2.json`，两轮均为 **177 套、2,354 项通过，0 失败、0 跳过**；类型检查、前后端构建及本轮相关代码规范检查通过。
- 新增测试文件：`tests/monthlyFinancialAnalysisChartInteraction.test.ts`（指标、线型、金额避让与滚动保持）、`tests/monthlyFinancialAnalysisStructure.test.ts`（已知明细结构及负值／未知边界）、`tests/monthlyFinancialAnalysisComparison.test.ts`（年份平移、两期隔离、权限及导出）。原面板、税费录入及连续历史图回归一并通过。
- 真实开发库对比查询的七模块金额与独立查询相同历史范围一致；对比导出 29 张工作表，逐格核对对比期金额通过。两期来源时间和人员告警不会互相串期，年度成本与离职人员保留；查询前后月报、手工项及月结快照摘要不变。
- 真实页面完成 2025 年默认对比、切换 2024 年、关闭对比及指标切换；同一时点的实线／虚线及圆点／菱形分别可辨认。初次或期间变化定位最近月份，普通刷新不夺走用户滚动位置。
- 视觉压力数据完全合成，不代表真实财务：`123,456,789,012,345,678.123456789012` 在本期和对比期标签均完整显示。几何检查对每个标签背景矩形逐对判定，无交叠；截图亦检查了实线、虚线、引导线和图例。
- 余额当前所选期末缺数据时，只为结构图引用所选范围内最近已知时点，并醒目注明日期。例如所选截至 2026-09、结构使用 2026-08；期末摘要仍为未知。负余额单列，绝不取绝对值画饼图。

本轮接口新增 `comparisonYear`（对比开始年份）及顶层 `comparison`（对比期），同连接只读取得两期、分别执行工资权限和内容版本检查；只读结构辅助不改摘要、明细、历史快照或导出原金额。尚未部署生产。

## 实现范围

- [x] 四账户余额、历史趋势、期末占比及明细。
- [x] 主营实际回款与一般利息、甲方及合同区域筛选。
- [x] 一般账户六类支出展示、已知来源分类及缺失口径提示，不把税费预留当支出。
- [x] 实际税费支付代码接入：复用月报手工项目“一般账户实际税费支付”，限定一般账户支出并要求金额、当月发生日期、说明和凭证号；纳入账户总支出与分析税费分类。历史无有效记录仍保持未知，不使用预留替代、不自动回填。
- [x] 主营项目合同额、已回款、未回款的历史截止统计。
- [x] 四账户结算收入、支出、净结余，不混用主营实际到账。
- [x] 商务报销实际付款月份、报销范围统计及明细。
- [x] 人员分项成本、历史离职人员、年度总额、四类公共费用按月分摊；无法证明的旧快照分项保持未知。
- [x] 月／季／年筛选，账户与未回款取期末，未知与零区分。
- [x] 精确金额、明细与导出一致、负余额及零分母保护。
- [x] 一致权限、只读自动刷新、保留不可变月结及现有连续历史交互。
- [x] 同步受影响的模块文档：`user/06`、`user/07`、`user/08`、`user/09`、`user/16`。

## 验证证据

- [x] 纯计算单元测试：精度、期间聚合、历史项目、分摊尾差、去重。
- [x] 服务和接口测试：查询约束、角色权限、月结快照、导出。
- [x] 真实组件测试：七模块、筛选、旧响应隔离、图表与明细、下载。
- [x] 现有趋势交互与月报回归测试。
- [x] 类型检查、相关代码规范、前后端构建。
- [x] 全量回归至少两轮，修复后重跑对应验证。
- [x] 开发库真实只读接口验证、真实组件合成数据界面验证，不写入生产业务数据。
- [x] 登录后的真实页面查询、月／季／年切换、组合筛选、明细分页、人员字段及原历史图交互。
- [x] 合成下载工作簿内存验证：已生成有效工作簿并重新读取，确认合成标识、查询参数和高精度金额原文保留；未读取或传输真实财务数据。
- [x] 浏览器文件实际保存：隔离浏览器连续两轮、每轮六场景完成真实下载；共12个文件实际落盘，压缩包及工作簿回读、金额精度、权限和响应摘要均通过独立复核。显式保存入口重试通过，版本冲突不产生下载。

### 最后一项：全自动浏览器下载验收

2026-09-02 使用 Chrome（谷歌浏览器）152.0.7977.66 的独立临时配置完成，无需人工保存。入口为 `debug/financial-browser-acceptance.html`，执行脚本为 `debug/accept-monthly-financial-browser-download.mjs`：

```bash
node --import tsx debug/accept-monthly-financial-browser-download.mjs
```

验收页使用纯合成数据和隔离回环服务，实际复用分析组件、请求封装、查询校验、工作簿生成及权限保护代码；不读取用户现有浏览器配置、登录信息或业务数据库。真实业务来源另外通过开发库只读检查验证，两者的证据范围分开记录。

| 场景             | 每轮工作表数 | 实际落盘及回读                 |
| ---------------- | -----------: | ------------------------------ |
| 无对比、全部模块 |           15 | 通过                           |
| 无对比、单模块   |            3 | 通过                           |
| 有对比、全部模块 |           29 | 通过                           |
| 有对比、单模块   |            5 | 通过                           |
| 显式保存链接重试 |            5 | 通过，独立文件且内容与前次相同 |
| 总经理、两期全部 |           29 | 通过，工资四项及特征值不可见   |

两轮报告：

- `/var/folders/7h/6q3ftdks295c3l_yvkshbgfh0000gn/T/financial-download-acceptance-XsNIDE/验收结果.json`
- `/var/folders/7h/6q3ftdks295c3l_yvkshbgfh0000gn/T/financial-download-acceptance-tfzJSs/验收结果.json`

脚本先等待浏览器下载完成事件，再从磁盘读取文件；没有直接写入报表文件。12个文件均非空、压缩包有效、可被电子表格读取器打开，文件摘要与对应网络响应逐字节一致；`123456789012345678.123456789012`、`2053235.40`、未知值及文本公式安全均保留。独立复核确认管理员完整文件含合成工资值，总经理两期文件不含该值或敏感表头。模拟版本变化返回冲突提示且无下载，浏览器没有脚本异常。隔离浏览器均已退出。

本轮另重新运行真实开发库只读检查，七模块两期金额、29表导出、角色权限及月报／手工项目／月结快照不变性均通过。新增自动验收脚本语法、规范及差异检查通过；业务代码未在此前两轮2,354项通过后修改。

### 前一轮执行结果

2026-09-02 16:32、16:33（北京时间）对最终代码连续执行两轮：

```bash
npm run test:run -- --runInBand --silent --json --outputFile=<本轮临时目录>/regression-1.json
npm run test:run -- --runInBand --silent --json --outputFile=<本轮临时目录>/regression-2.json
```

两轮均为 **172 套、2,272 项通过，0 失败、0 跳过**。本轮临时结果目录为 `/tmp/monthly-financial-final-20260902.hfdJc8`；该目录仅用于内部验证，不含导出的真实财务明细。随后读取两份机器结果确认计数，未仅凭进程结束判断成功。

- `npm run type-check`：通过。
- `npm run build:server`、`npm run build`：通过。
- 针对本次新增及修改的分析服务、路由、类型、组件、接口封装、测试、检查脚本运行代码规范检查：通过。
- 构建仍有现有依赖／环境提示（样式预处理旧接口、较大资源包、环境变量提示）；测试运行有本机图像库重复加载及本地存储参数提示，未导致失败。本次未为消除这些无关提示升级依赖。

### 登录后修复与最新执行结果

2026-09-02 用户登录后，在真实开发页面发现并修复两个展示／交互问题：

1. 部分来源更新时间显示为无效文字。分析服务现在只接受合法日期，按实际时刻而非字典序比较，无时区业务时间按北京时间解释；无合法时间显示未知。来源更新时间不采用每次查询生成时钟，因此同一来源的导出版本保持稳定。未修改原银行交易记录。
2. 自动下载未在用户电脑出现。页面不再点击后立即回收文件地址，而是最多保留一个明确的“保存文件”链接；查询或版本变化、替换文件、组件卸载时回收。确定返回未登录／无权限时清除已加载数据及文件入口；自动点击被拒绝时仍可保存，不误报文件生成失败。导出过程中查询发生变化时，不提供旧结果的保存入口。

17:46（北京时间）对本轮最终代码连续两轮运行 `download-regression-1.json`、`download-regression-2.json`，均为 **173 套、2,292 项通过，0 失败、0 跳过**。结果保存在前述临时目录。总数包含当前工作区其他既有／并行功能测试，不将全部测试数归为本次新增。

类型检查、前后端构建、本次变更文件的规范检查再次通过；开发库只读检查再次通过，七模块导出逐格一致、版本稳定且月报／手工项目／月结快照内容不变。新增的前端下载生命周期与竞态测试已包含在最新全量回归中。

### 实际税费接入后的最终执行结果

2026-09-02 18:09（北京时间）对包含实际税费支付及下载保存入口的最终代码再次连续运行 `tax-final-1.json`、`tax-final-2.json`。两轮均为 **174 套、2,300 项通过，0 失败、0 跳过**。随后类型检查、服务端构建和前端构建全部通过。

- 税费相关 10 套、175 项测试单独通过；覆盖分类、强制账户／方向、双必填、精确金额、账户闭合、分析明细、凭证号导出、原月报公式及前端只读权限。
- 开发库实际约束 `monthly_financial_manual_tax_evidence_check`（税费凭证约束）已验证；合法映射为真，错误账户和收入方向为假，历史税费记录仍为 0。
- 开发库七模块只读接口检查再次通过：15 张工作表逐格一致，角色门禁正确，查询前后月报、手工项目和月结快照摘要不变。
- 真实登录页面显示“一般账户实际税费支付”入口；新增客户端行后显示业务说明及凭证号必填控件。验证页未点击保存，没有写入测试业务记录。
- 该轮合成导出在浏览器内生成 8,961 字节有效工作簿并立即回读，确认 1 张表、`2053235.40` 及 18 位整数＋12 位小数原值完整，真实业务请求为 0。当时尚无文件落盘证据，现已由上方最终自动浏览器验收补齐。

### 需求与行为测试对应

以下均为可执行金额或交互断言，不仅是检查源码包含某个词。

| 需求                 | 证据文件及关键断言                                                                                                                                                                                                                                                                |
| -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 四账户余额及收支聚合 | `tests/monthlyFinancialAnalysis.test.ts`：完整七模块；季度结算为 464／244／220，余额取期末；全年结算为 2784／1464／1320，年末高精度余额为 2225.123456789012，不累计十二个月余额                                                                                                   |
| 入账与筛选           | 同上：主营实际回款和利息不被账户拆分核算基数替代；甲方筛选不把无归属利息伪造成零                                                                                                                                                                                                  |
| 支出分类与去重       | 同上：报销行政分类、资产、手续费不重复；税费预留不当实际付款；实际税费现已映射既有手工分类，说明／凭证号不完整或历史未录继续未知。新增税费界面及后端校验用例见 `tests/monthlyFinancialTaxManualFrontend.test.ts`、`tests/monthlyFinancialReportBackend.test.ts`，最终运行计数另记 |
| 项目历史截止         | 同上：补充协议、冲正、超额回款、可信合同日期；跨年合同 1000／1000、累计回款 500／600、未回款 500／400                                                                                                                                                                             |
| 商务付款归月         | 同上：只纳入实际付款的商务报销，排除其他类型及下月付款，按报销范围筛选                                                                                                                                                                                                            |
| 人员成本与费用分摊   | 同上：工资组成与原总成本闭合、同名不合并、离职人员保留；8 月 7 人、9 月 6 人分月分摊，季度总额 220；四类费用、无工资名单、跨年多次付款尾差均有断言                                                                                                                                |
| 精度、未知与导出     | `tests/monthlyFinancialAnalysisContract.test.ts`：生成并回读真实工作簿，保留 18 位整数＋12 位小数，单小数补零；未知不按零导出，文本不执行为公式                                                                                                                                   |
| 权限与快照           | `tests/monthlyFinancialAnalysisPermissions.test.ts`、`tests/monthlyFinancialAnalysisSnapshot.test.ts`、`tests/monthlyFinancialAnalysisRoutes.test.ts`：管理员保留工资分项，总经理响应和文件移除敏感分项；冻结维度校验；开放月只读重算，事务失败回滚                               |
| 七卡片及请求交互     | `tests/monthlyFinancialAnalysisFrontend.test.ts`：真实挂载、筛选、迟到响应、可见页面 60 秒刷新、未提交条件保护、卸载清理、已显示版本导出、分页及无工资权限图表                                                                                                                    |
| 连续历史交互         | `tests/monthlyFinancialTrendChart.test.ts`、`tests/monthlyFinancialTrendRange.test.ts`：连续月份、拖动／滚轮／键盘、上年同期、24 个精准标签、全屏、跨年分段                                                                                                                       |

### 开发库与浏览器验证

只读检查脚本：`debug/check-monthly-financial-analysis-20260902.ts`。必须在开发容器并显式启用，临时服务仅监听回环地址、只接受读取请求，不建立持久登录会话。

```bash
docker exec -e ALLOW_FINANCIAL_ANALYSIS_READ_CHECK=true yulilog-worklog-dev npx tsx /app/debug/check-monthly-financial-analysis-20260902.ts
```

实际运行通过：

- 截止月份 2026-08，返回七个模块；明细行数分别为账户 32、入账 5、出账 45、项目 458、结算 32、商务 1、人员 49。
- 全部导出 15 张工作表；逐单元格核对七模块全部明细与期间金额，和同一版本接口响应一致。
- 月／季／年请求均成功；相同来源版本稳定，旧版本导出返回冲突，不下载与页面不一致的文件。
- 使用真实认证中间件核对匿名、董事长、经营管理角色、普通员工的查询及导出拒绝；管理员查询／导出成功；总经理仅获得授权字段，人员导出不包含工资四项。
- 查询前后，真实开发库月报、手工项目、月结快照的完整内容摘要一致。
- 浏览器直接挂载真实分析组件的合成数据预览：七卡片、季度切换后各 4 期、高精度与补零金额、未知／负值提示、明细分页及图形布局完成检查。此预览明确不读取真实财务数据，不能代替登录后整页验收。
- 用户随后以管理员登录，已完成真实页面核验：七模块可见；同一单月范围切换月／季／年后七模块金额一致；甲方＋合同区域筛选后的项目明细匹配，第二页 50 行与第一页不重复；人员分项权限和至少两位小数正常，无名单的公共费用作为独立提示保留，不假归给所选人员。
- 原历史区可展开、开启同期对比、进入／退出全屏；键盘跳至历史起点和终点时实际滚动位置分别为 0 和完整可浏览宽度，原跨年浏览能力未丢失。页面错误日志检查未发现运行错误。
- 合成下载工作簿已在内存中生成并重新读取，确认工作簿可解析、查询信息与合成高精度金额保留；这项验证没有使用真实财务明细。
- 早期内置浏览器检查未能验证文件保存：接口与内存工作簿已通过，但工具不暴露下载完成及磁盘路径。现已改用隔离浏览器取得实际下载事件、磁盘文件和独立回读证据；没有将早期“无页面报错”或内存回读成功冒充文件送达。

## 验收后的数据边界与发布状态

1. **实际税费历史数据**：来源代码已完成，管理员可在既有月报手工项目录入“一般账户实际税费支付”，限定一般账户支出并必填金额、当月发生日期、说明与凭证号。代码完成不代表历史税款已补录；无有效记录月份继续未知，主营税费预留不转为付款，旧月结快照不回填、不改写。
2. **浏览器文件保存**：已由隔离浏览器自动完成真实落盘与回读验收。内置浏览器原先不能给出可靠下载证据，此限制没有被当作通过依据，也不再作为待用户确认项。
3. **历史数据**：旧月结未冻结工资组成、来源无法归类、缺少汇率或尚未发生的未来期间，保留未知并提示原因，不能用当前值重写旧事实。
4. **权限**：工资四项沿用现有工资模块管理员权限；总经理可查看已有授权成本合计和费用，不默认扩大薪资权限。
5. **发布**：本次未部署生产。工作区还有此前其他功能的未提交修改，未重置、覆盖或打包为本次单独发布。

## 当前实施约定

- 按后续合并意见组织七模块，一模块可以包含多个图表。
- 甲方／合同区域仅筛选存在可靠归属的记录；报销范围单独筛选，无法归属的公司费用保留并明确说明。
- 沿用当前月报精确角色权限，不新建人员资金子账或复杂权限体系。
- 自动更新采用定时只读查询，不在后台调用会改版本或关联凭证的月报写入接口。
- 暂无可核验实际来源的分类显示未知及原因，不制造零值或将计提额冒充付款。
- 实际税费不新建业务表或税务流程，沿用现有手工项目保存接口及分类约束；新增分类和说明／凭证号检查迁移尚未在生产部署。

## 更新日志

- 2026-09-02: 建立完整需求验收清单及当前实施约定；后续以代码、测试和实际运行结果逐项记录证据。
- 2026-09-02: 补记最终两轮各 2,272 项回归、年度金额边界、真实开发库逐格导出核对与快照不变性；保留实际税费来源和登录态整页验收两项未完成，不将未知数据或合成界面测试当作全部验收。
- 2026-09-02: 用户登录后完成真实筛选、分页、人员与历史图交互；修复无效来源时间和导出文件地址过早回收，补充持久保存入口及权限／竞态清理；最新两轮各 2,292 项通过，剩余实际税费来源与文件实际保存确认。
- 2026-09-02: 实际税费支付复用月报手工项目完成代码接入，同步界面必填、接口校验、分类约束和服务映射文档；历史无凭证记录继续未知。补记合成工作簿内存生成及精度回读，浏览器实际落盘仍无工具证据，生产未部署；保留此前测试数字，最终回归计数另补。
- 2026-09-02: 实际税费及下载入口最终代码连续两轮各 2,300 项通过；类型检查、两端构建、开发库真实约束、七模块只读接口及合成工作簿回读通过。未写入测试业务记录，未部署生产；仅待用户确认浏览器实际保存文件。
- 2026-09-02: 新增用户反馈五项整改检查：模块切换、首屏压缩、默认往年对比及指标、精准标签避让、已知正金额结构口径；同步双期查询、工资权限及 29／5 表导出，无比较仍 15／3 表。视觉项暂不勾选，既有测试数字不改，本轮验证另记，未部署生产。
- 2026-09-02: 五项整改完成宽窄屏和真实数据验收，双期同值／超长精度／四线44标签无碰撞；对比29表与独立历史查询金额一致。最终两轮各2,354项通过，类型、构建及规范检查通过；未部署生产，文件实际保存仍待用户确认。
- 2026-09-02: 最后下载项改为全自动验收：隔离浏览器连续两轮共12个文件实际落盘并独立回读，覆盖15/3/29/5表、显式保存重试、工资权限及版本冲突无下载。全部验收项完成，不再等待人工保存确认；未部署生产。
