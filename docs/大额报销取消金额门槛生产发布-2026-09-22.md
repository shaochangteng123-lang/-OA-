# 大额报销取消金额门槛生产发布

2026-09-22 已完成生产发布。生产固定运行镜像为 `yulilog-prod-yulilog:large-reimbursement-no-limit-20260922-v1`，镜像摘要为 `sha256:5b44c69f15cdd617d2cbdb3602566a948c0cdfe50612419f625ea4b72631be5c`。应用、数据库与 Nginx（反向代理）均健康，应用和数据库重启次数均为零。

## 发布范围

- 大额报销前端移除1000元门槛配置、金额不足提示和编辑重提拦截。
- 大额报销创建及编辑接口移除低于1000元的拒绝逻辑。
- 个人大额报销列表移除 `total_amount >= 1000` 金额过滤，低金额记录不再因状态而隐藏。
- 通用发票正数金额、重复票、购买方、核减、权限和状态校验保持不变。
- 不包含数据库迁移，不修改已有报销、发票、审批、付款或上传文件。

## 构建边界

当前工作区存在其他未提交功能，未从工作区整包构建。候选以前一生产镜像 `yulilog-prod-yulilog:team-image-preview-20260922-v3` 为基础：

- 前端从上一版受控生产源码快照隔离构建，只覆盖 `InvoiceTable.vue`、`constants.ts`、`typeConfig.ts` 和 `LargeReimbursementDetail.vue` 四个报销文件。
- 服务端只覆盖 `/app/dist/server/routes/reimbursement.js`；与生产基线逐行比较，差异严格为创建、个人列表和编辑重提三段1000元门槛删除。
- 服务端候选文件 SHA-256（安全散列算法）摘要为 `c586cb890c16c924043b1a951a2b6496b7b2d382c575d940e2f9136d27ee71fd`。
- 活动前端入口为 `index-CFFbr6cw.js`，大额报销配置为 `typeConfig-FJPtrR6P.js`，详情页面为 `LargeReimbursementDetail-DkaThUKL.js`。

## 验证结果

- 完整类型检查通过；298个测试套件、3970项测试全部通过。
- 隔离前端生产构建通过，源码范围审计仅发现上述四个报销文件变化。
- 候选与正式生产均确认服务端不存在旧金额判断及列表过滤，活动前端不存在金额门槛属性、警告文案或提交拦截。
- 应用上游与反向代理 `/api/health` 均返回成功；生产镜像摘要、活动前端入口及候选逐项一致。
- 上线前完整数据库备份位于生产数据库持久卷 `/var/lib/postgresql/data/predeploy-backups/large-reimbursement-no-limit-predeploy-20260922-v1.dump`，文件大小1,329,163字节，SHA-256摘要为 `8021fe4b1cf58b36d3e4b7cba3c4d47ef0b5ecf63919f064b8cb95610bcd8b3d`，恢复目录校验通过。
- 报销、发票、审批和付款相关表上线前后指纹摘要均为 `1545b6dd01f61b5440cca0e3f37085bf86f971d4a20130a5e05fbaf8d6b48cbd`，内容完全一致；现有一笔988.45元大额草稿仍完整保留。
- 全库指纹仅出现一项正常并发变化：`daily_logs` 从379行增加至380行。该空白草稿创建于 `2026-09-22T03:25:15.753Z`，早于新容器 `2026-09-22T03:25:43.584Z` 的启动时间，不是发布副作用。
- 正式终验仅执行只读检查，没有代用户创建、提交或修改报销单。

## 回滚

受控发布与回滚配置保存在 `backup/large-reimbursement-no-limit-release-20260922.v1/`。上一生产镜像 `yulilog-prod-yulilog:team-image-preview-20260922-v3` 保留。若需要回滚，只切换应用镜像并重载 Nginx（反向代理），不恢复数据库、不删除上传文件。回滚旧版后，新产生的低金额大额报销可能再次被旧列表规则隐藏或在编辑重提时受限，但数据不会丢失，应优先前向修复。

## 更新日志

- 2026-09-22: 大额报销取消1000元最低金额门槛并正式上线；隔离构建、完整回归、健康链路、生产活动资源、备份与报销相关数据不变核验全部通过。
