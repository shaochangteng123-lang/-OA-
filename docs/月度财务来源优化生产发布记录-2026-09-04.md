# 月度财务来源优化生产发布记录

## 发布结果

按用户“先发布，因为生产数据是正式的”的要求，2026-09-04 16:12（上海时间）完成生产代码切换。应用健康、反向代理正常，生产健康接口成功，未登录访问分析接口仍返回401。

**只发布程序，不改业务数据。** 未导入开发数据库或开发附件，未补写55笔报销的付款日期、1笔已核验人民币币种或另外4笔未确认币种，没有执行历史修复脚本。原金额、关联关系及月结快照未改变。

发布镜像：`yulilog-prod-yulilog:finance-sources-20260904-n6kr7k`。

镜像摘要：`sha256:c05b971f54c364aa08482d7c9815638afe14a98b74b200a521988e284ad0bcf8`。

## 范围与保护

- 基于当前安全生产版本 `finance-label-20260904-bemre4`，只覆盖三个财务分析服务，新增公共费用范围与报销日期诊断两个服务，前端资源整套同步。
- 其余201个后端文件、启动迁移、月报路由、工资计算器及五份原服务静态资产保持字节一致。原有发票方向关联不被启动迁移重写，账户字典无变化时不更新时间。
- 保留284份旧前端资源供尚未刷新页面使用。未改变生产环境变量、数据库外部卷、附件挂载、网络或代理配置。
- 只切换应用容器并重载原代理，数据库及开发服务未重建。

## 发布前验证

- 既有最终回归218套、3,132项全部通过；前后端类型、构建及定向规范检查通过。
- 使用最新正式生产备份恢复独立网络、独立数据卷的隔离库。55张相关表、3,449条记录在完整初始化前后、查询与导出前后的内容摘要完全一致。
- 月度查询2026年1—9月、季度查询7—9月、年度查询1—9月，均返回七模块及上年同期。工资独立序列、商务已归期金额、缺日期提示、总经理双期工资分项裁剪均通过；整套导出成功。
- 隔离演练初次因验收脚本位于应用目录外无法解析依赖而失败，修正为应用目录内的只读脚本挂载后全流程通过；没有改动候选应用代码，也没有因此操作生产数据。

## 生产界面核验

用户原有登录态可继续使用。实际打开生产页面并核对：

- 人力默认显示“工资总成本（含公司缴费）”，1—9月有九个真实金额点：1月68,987.26元，2—6月各69,987.26元，7月70,107.26元，8月70,683.97元，9月70,107.26元；期间工资合计629,822.05元。点旁精准金额清晰，缺工资来源的2025年月份保持未知。
- 商务明确显示“已归期部分”，8月17,593.70元、9月20,600.00元、已归期合计38,193.70元。页面提示25笔商务报销68,789.12元缺有效付款日期，明确为全历史诊断，不冒充本期金额或宣称已恢复4—7月数据。基础及大额缺日期信息亦保留。
- 项目选择“本月回款”后，本期可见2025年10月至2026年9月十二点，真实零显示¥0.00；同期缺币种的2024年11月、12月和2025年6月保持未知。关闭年度对比后同期点为零，恢复后正常显示。
- 全屏内七模块均能切换，退出全屏正常。浏览更早一个月时，本期窗口2025-09至2026-08对应同期2024-09至2025-08；随后恢复原窗口，未改上方统计范围。
- 最后保持人力成本工资曲线视图。没有进入会生成工资的人力维护接口，没有同步月报、编辑金额或补写日期／币种。

## 正式数据不变证明

正式切换前、切换后以及页面验收后，对55张财务相关表逐行计算内容摘要，三次结果完全一致，整体摘要为：

`8d7ea9128283c187925527c6e2eca0103690a42ae58109a3f45f2c80295b473a`。

范围包括合同、票款与发票明细、月报及审计、工资、全部报销相关表、付款批次、旧银行回单和员工资料。另分别核对报销日期缺口和回款币种缺口，均保持原值。

## 备份、回滚与清理

私有目录：`backup/financial-source-release-20260904.n6KR7K`，目录权限700。

- `production-before.dump`：隔离演练用正式备份。
- `production-cutover-before.dump`：实际切换前的最新正式备份。
- `production-dist/`：原生产程序及资源。
- `prepare-manifest.json`、`verified-image.json`：镜像、差异、环境一致性及产物核验。
- `business-cutover-before.txt`、`business-cutover-after.txt`、`business-after-ui.txt`：正式数据指纹。
- `source-gaps-*.json`：历史日期／币种保持原值的检查。
- `preflight-result.json`：隔离演练成功结果。
- `compose.release.yml`：本次固定版本；`compose.rollback.yml`：回退到本次之前的安全生产版本，不恢复数据库。

隔离容器、独立网络及仅存备份副本的测试数据卷已清理；生产资源、正式备份和两版镜像保留。不得为了回退代码而覆盖正式数据库。

## 后续数据边界

本次是代码发布，不是历史数据修正。缺付款日期、缺币种或未明确公共费用归属的记录继续待核对，不补零、不猜值。若后续批准补齐，应基于原件和最新并发核验记录另行执行，不能把本次发布当作补写授权。

## 更新日志

- 2026-09-04: 此次代码发布之后，用户另行确认25笔商务日期补齐，已完成独立事务修正；该数据操作不属于16:12代码发布，详见《商务报销历史付款日期补齐记录-2026-09-04》。
- 2026-09-04: 完成财务来源与人力分项优化的代码生产发布，真实页面验收及55张业务表不变检查通过，历史字段未补写。
