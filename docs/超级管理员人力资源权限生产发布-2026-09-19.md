# 超级管理员人力资源权限生产发布

2026-09-19 已完成生产发布。生产应用固定运行镜像 `yulilog-prod-yulilog:hr-superadmin-20260919-xdaxge`，容器状态为健康；反向代理重载、正式只读权限接口、公开前端资源和切换后业务数据摘要均已完成终验。

## 发布范围

- 人力资源审批中心精确开放给总经理和超级管理员；普通管理员及董事长不获得该独立工作台。
- 超级管理员可使用本人锁定签名处理仍分配给活动总经理的转正主管环节，并保留本人原人事部任务；实际签署人写入既有签名记录，不能代董事长终审。
- 超级管理员可处理活动总经理名下的非总经理请假，禁止自审及代审需董事长处理的总经理请假；原总经理审批归属不改写，审批日志保存实际超级管理员，原总经理可查看代审结果。
- 超级管理员可读取统一的请假待办、审批记录、年度／月度员工统计和明细；待办角标与列表使用同一范围，普通管理员权限不扩大。
- 转正签署增加预期环节与表单版本校验，过期页面、重复点击或并发推进不能跨阶段写入。

候选镜像以 `yulilog-prod-yulilog:leave-20260918-qewynm` 为基线，基线镜像编号为 `sha256:19bcb23077437e29d10f9161193a3ff1cdbe045773a512ac025fb95549e28005`；新镜像编号为 `sha256:d863595b1f302e129953641a4dc6e35c09c48f16cc8d55260d07e964cd421001`。服务端仅 6 个权限相关构建文件变化，数据库模块与应用总入口逐字节不变；前端源码差异限定在 6 个已批准的人力资源文件。

## 测试与隔离验收

- 完整测试共 288 套、3,876 项，全部通过；前后端类型检查与构建通过。
- 候选完整启动、只读验收前后，隔离数据库全部业务表指纹一致。
- 隔离写验收实际执行了超级管理员通过、驳回、自审阻断和总经理申请阻断，核对审批日志记录真实超级管理员，并确认原总经理可以读取代审结果。
- 隔离验收中超级管理员与总经理统计均为 6 名员工、4 次已批准申请、14 天；普通管理员、董事长及普通员工的统计权限保持隔离，待办计数与待办列表一致。
- 历史病假附件经受控下载后摘要与原文件一致，原有请假申请内容在验收前后未变化。

隔离写验收只连接 `hr_release_check`（人力资源发布核验库），不连接或写入生产数据库。

## 生产终验

- 生产容器运行新镜像且健康，开发应用、生产数据库、开发数据库和反向代理未受切换影响。
- 正式只读权限验收再次确认超级管理员与总经理均为 6 名员工、4 次申请、14 天；普通管理员和董事长统计返回 `403`，普通管理员访问请假待审批列表同样返回 `403`，超级管理员待办计数与列表一致。
- 正式历史病假附件下载摘要一致。生产验收未执行通过、驳回或其他业务写入。
- 首页及 8 个关键 JavaScript（脚本）／CSS（层叠样式表）资源与候选文件逐字节一致，反向代理重载成功。
- 上线前、切换后和正式只读验收后的全部业务数据摘要均为 `b8438f7d24bad070c3c0730b3e909b0054050abc3f26db21733bfe2bee143f0c`，与备份一致；本次不新增数据库表、字段、索引或迁移，也未修改既有业务数据。

## 备份与回滚

发布材料保存在 `backup/hr-superadmin-release-20260919.XDaxGE/`：

- `production.dump`：切换准备阶段完整备份，大小 1,315,455 字节，权限 `600`，SHA-256（安全散列算法）摘要为 `d7012b5c3c8b4a3e7227368ffcaa4289ec09153f9c4fc42bb39b44ff3bdb789b`。
- `production-cutover.dump`：实际切换前完整备份，大小 1,315,454 字节，权限 `600`，SHA-256（安全散列算法）摘要为 `ef14ca99383cc17a96fa8a54c5a0a72a5247f2938282d09c5b0fe16f6c5ec2e4`。
- `rollback.yml`：固定指向上一版镜像 `yulilog-prod-yulilog:leave-20260918-qewynm`。

应用回滚命令：

```bash
docker compose -f docker-compose.prod.yml -f backup/hr-superadmin-release-20260919.XDaxGE/rollback.yml up -d --no-deps --no-build --wait --wait-timeout 180 yulilog
docker exec yulilog-nginx nginx -s reload
```

应用回滚只切换应用镜像，保留生产数据库卷和上传附件。由于本次没有数据库结构或业务数据变更，不应使用切换前备份覆盖上线后的正常业务数据；完整备份仅用于独立恢复核验或经明确授权的数据库事故恢复。

## 证据与清理

- 镜像逐文件核验：`final-image-readonly-audit-20260919.md`
- 完整测试：`release-check.log`
- 隔离权限及实际审批：`results/isolated-permissions.json`
- 正式只读权限：`results/production-permissions.json`
- 正式公开资源：`results/public-assets.json`
- 数据摘要：`production-before-verified.json`、`production-after-verified.json`、`production-after-read-verified.json`

生产终验完成后，生产容器内临时验收脚本和空目录已精确删除；本次专用隔离应用、数据库容器、网络及数据卷已清理。本地备份、报告和可重建验收材料继续保留。

## 更新日志

- 2026-09-19: 超级管理员人力资源权限正式上线；生产镜像健康，3,876 项测试、隔离实际审批、正式只读权限、公开资源、反向代理和全部业务数据不变校验通过。
