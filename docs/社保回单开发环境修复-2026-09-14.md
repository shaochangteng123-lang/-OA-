# 社保回单开发环境修复

开发环境此前持续运行旧目录挂载，容器中的工资路由仍为社保识别版本 3，且缺少遮挡收款账号兼容。重建挂载后发现主工作区合同源码早于开发数据库中的“非主营业务支出合同”分类，数据库约束迁移中止，不能直接使用当前源码启动开发后端。

处理方式如下：

- 重建前完整备份开发数据库；失败迁移后使用该备份恢复，未删除开发业务数据。
- 开发应用暂时切换到“2026-09-14 当前生产基线＋社保修复”的候选镜像，仍连接开发数据库及开发附件目录，生产环境未切换。
- 开发服务当前健康，社保识别版本为 6，遮挡账号兼容和电子回单号查重服务均已加载。
- 用户提供的两张补打回单在实际开发容器中分别识别为 19,834.44 元和 40.17 元，电子回单号分别为 0920-5144-9671-1100 和 0920-2021-0505-1100。
- 主机访问开发地址及健康接口返回正常。没有替用户上传回单，也没有修改现有失败记录。

开发运行配置位于 `backup/hcr-dev-remount-20260914/compose.fixed-runtime.yml`，数据库备份位于同目录 `development-before.dump`。当前开发运行方式用于验证已发布代码与本次修复，不提供后端源码热更新；待主工作区同步 2026-09-11 后合同源码后，可恢复标准开发容器。

生产候选镜像仍未上线，等待明确生产发布授权。

## 更新日志

- 2026-09-14: 恢复开发数据库并切换社保修复候选运行环境，两张补打回单实际识别和开发地址健康检查通过。
