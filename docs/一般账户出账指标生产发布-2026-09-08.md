# 一般账户出账指标生产发布

## 交付状态

北京时间2026-09-08 10:14完成生产切换，页面及数据不变验收通过。

- “一般账户跨行手续费”仅取一般账户有效跨行手续费。
- “行政支出”为基础报销＋大额报销；有未归期付款时明确标注已归期部分。
- 下拉及多指标比较仅显示行政、薪资、跨行手续费、实际税费、资产类合同五项，移除业务支出、一般账户总支出、一般账户支出（已录入部分）三个可选项。原账户摘要和饼图中心权威总额保留用于核对，不改变原账。
- 分类按自身来源判断完整性，整体差额不再清空已核验分类；未知不补零，其他杂项不混入手续费。

## 验收结果

- 242套、3,354项完整回归及前后端构建、类型检查通过。
- 最新正式数据副本启动迁移未改动原始数据；月度、季度、年度、明细及出账导出一致，其他六模块本期／同比响应与生产基线严格一致。
- 生产页面下拉及多指标均五项；2026年8月手续费9.00元、已归期行政支出10,331.05元。
- 手续费点位点击后保持该指标与检查器，结构图切到2026年8月，手续费9.00元和行政10,331.05元一致。
- 生产导出显示“文件已生成”，可点击保存。隔离接口实际返回并核验5张工作表；没有将生成成功说成用户端下载完成。
- 30笔未归期报销保留：26笔基础报销41,188.69元、4笔大额报销10,800.00元，合计51,988.69元。没有擅自补付款日期、改用报销月份或塞入某月。

## 数据保护与发布范围

初次切换前门禁发现`payroll_records`（工资记录）在生产有并发更新，自动停止，尚未切换服务。随后重新备份最新数据，备份前后指纹稳定，再恢复到第二套独立演练库，重新通过完整接口与数据不变验收后发布；没有用旧备份覆盖生产数据。

切换前、切换后及生产页面验收后，55张财务业务表内容摘要均为：

`8e0ccefc38db7407b6ddf7bea52eada894c876e7defc5f981917da09aab9edf9`

仅替换分析主函数所在服务文件以及财务页面构建资源；其他209个后端文件逐字节保留，1,095份旧前端资源保留。原有启动保护、住房发票租期分摊、项目数量及名单功能保持不变。

## 镜像与回滚

- 原生产：`yulilog-prod-yulilog:housing-invoice-20260907-jph5yj`。
- 新生产：`yulilog-prod-yulilog:outflow-metrics-20260908-juf4om`。
- 新镜像摘要：`sha256:15e22f8bb61c039c420d379ffbe1b101fe9601e3d1140a266463be7e4b312b5f`。
- 资料目录：`backup/outflow-release-20260908.JUf4om/`。保留初始、刷新和切换前正式备份、镜像逐字节校验、测试结果、接口导出、浏览器记录以及业务表指纹。
- 重启本版本使用该目录`release.yml`结合`docker-compose.prod.yml`；需要回滚应用时使用`rollback.yml`并重载反向代理，不恢复覆盖数据库。
- 本轮两套临时测试容器和测试卷已清理，正式备份、发布及回滚镜像保留，可重建测试副本。

## 更新日志

- 2026-09-08: 一般账户出账五指标及独立来源取数正式上线，生产点选、导出与55表数据不变核验通过；未归期报销如实保留。
