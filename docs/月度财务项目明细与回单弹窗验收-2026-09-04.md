# 月度财务项目明细与回单弹窗验收

日期：2026-09-04。仅开发版调整，未部署生产。

## 已完成需求

- 已回款与本月回款统一为按行政区分组、项目标题下横向金额卡片的明细形式，不再使用右侧常驻回单双栏。
- 已回款显示“已回款、合同金额、未回款”三项，无银行回单入口。
- 本月回款显示“本月回款、合同金额、已回款、未回款”四项，仅本月回款使用突出背景色；季度／年度第一项按粒度改名，窄屏以两列展示。
- 仅期间回款指标显示“银行回单预览”文字入口，点击弹窗查看图片或文档，并支持同项目多张回单切换；未打开不请求原件。
- 弹窗关闭、切换期间／指标／模块或所选项目移出正回款清单时清理旧预览，不自动重开。原生全屏内可以查看，退出键先关闭回单，不同时退出分析全屏。

## 数据与影响范围

仅修改项目明细、预览弹窗、父面板弹窗协调及对应测试和文档；新增组件声明由构建生成。后端、接口、数据库、权限及导出没有变更。

四项金额直接使用同一期间的原始字段：期间实际回款与同期期末合同额、累计已回款、未回款分别展示，不互相反推。真实零显示 `¥0.00`，高精度金额完整保留，未知不补零；预收款仅流量行的三项存量仍未知。本期清单仅列正向回款项目，权威总额不由可见项目重算，累计清单不套用本期过滤。

## 验证结果

- 本轮弹窗与原预览专项：58 项通过；项目明细与面板联动专项：50 项通过。补充真实隐藏宿主回归，覆盖原生关闭自动回焦后入口才隐藏的时序，以及不抢走新焦点。
- 全量测试：203 套、2,868 项全部通过。
- 前后端类型检查、生产构建和本轮文件静态规范／格式检查通过。构建仅产生本地文件，未执行部署；保留既有大文件体积提示。
- 隔离浏览器专项：8 项通过，未读取真实业务数据库，未触发下载，未出现浏览器异常。
- 已人工查看本期四卡、累计三卡、图片弹窗、文档正文、原生全屏及390像素窄屏截图，确认金额未截断、弹窗未外溢。

测试结果：`/tmp/financial-project-receipt-dialog-final-20260904.json`。

最终浏览器报告及7张截图目录：`/var/folders/7h/6q3ftdks295c3l_yvkshbgfh0000gn/T/financial-download-acceptance-cutSte`。两轮相同8项均通过，最后一轮覆盖焦点补丁后的冻结版本。目录内为合成验收资料，不是真实银行回单；临时目录可能被系统清理。

## 更新日志

- 2026-09-04: 完成横向金额卡片与银行回单点击弹窗需求的开发和自动验收，未部署生产。
