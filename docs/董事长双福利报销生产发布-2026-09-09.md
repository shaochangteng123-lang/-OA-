# 董事长双福利报销生产发布

## 状态

2026-09-09 17:47（北京时间）完成生产镜像切换，17:58 完成生产数据、接口、前端资源、月报模板和回滚材料验收，现已上线。

## 上线范围

- 董事长基础、大额、商务、福利1、福利2五类报销免审批直达待付款；付款与付款回单仍仅允许管理员和超级管理员操作。
- 福利2入口位于福利1下方，复用大额报销布局并使用柔和玫瑰色；审批中心、全部查询、统计、付款、角标及详情路由均已接入。
- 福利1、福利2使用独立分类表。福利1顺序为饮用水、办公、电费、407-AI、8H-AI、其他，其中407-AI和8H-AI默认停用；福利2默认茶歇、团建、体检。
- 两类分类均支持新增、编辑、排序、停用和删除。未引用分类可物理删除；存在报销、动态月报手工项或旧固定手工项引用时返回 `409` 并提示停用。删除标记阻止默认分类在重启时恢复。
- 福利报销按付款业务日期进入对应福利账户；自动报销在月报手工区域只读显示，并与有效手工金额按分类相加。月报公式升级为 `monthly-finance-v6-dual-welfare-reimbursement-categories（第六版双福利报销分类公式）`，旧月结快照继续兼容。
- 月度财务下载模板同步使用确认后的柔和配色、放大标题和合并单元格；福利1、福利2分类行可分别动态扩列，公式和打印区域随较长分类行调整。

## 构建与发布

- 回滚基线镜像：`yulilog-prod-yulilog:outflow-total-20260909`，镜像编号 `sha256:0c1e8d0bf3633584f7b40c7758efb722e472c4d0b855a5db9c573a34f8fba4b7`。
- 当前生产镜像：`yulilog-prod-yulilog:dual-welfare-20260909`，镜像编号 `sha256:732c4728401fe74bdeef54474b818c7276a7cf4f730d431c1c1b318396fe4c90`。
- 发布前执行完整 `release:check（发布检查）`：250套、3427项测试全部通过，前后端类型检查通过；生产构建与镜像内服务端构建通过。
- 使用 `docker-compose.prod.yml（生产编排配置）`与本次发布包的 `release.yml（发布覆盖配置）`执行不可变镜像切换，等待应用、数据库和反向代理全部健康后重载 `Nginx（反向代理）`。

## 备份与隔离演练

发布材料位于 `backup/dual-welfare-release-20260909.31Pzff/`，目录权限为700，数据库和附件备份权限为600。

- 正式切换时点数据库备份：`production-final-before.dump`，`SHA-256（安全散列算法）`摘要为 `3fab3b4c9cc300598e3bad4700130bd0dfd92097772c713109cc597513c65532`，已通过 `pg_restore（数据库恢复工具）`清单校验。
- 正式切换时点生产附件备份：`production-uploads-final.tar.gz`，摘要为 `fd162aafdf7e2fa12325f59ec94c8fd29881acc89d320823efeaaf1ca4274703`。
- 候选源码备份：`candidate-source.tar.gz`，摘要为 `04cced5bfc8dfbf46933ae9afc3bb587b60374b60d18f6c1d1558647b62524f4`。
- 常规完整备份脚本因开发上传目录已有一份 `NaN/NaN/NaN` 错位附件而主动停止；未移动该开发文件，也未把失败结果冒充有效备份。本次改用独立生产数据库、生产附件和候选源码备份完成切换保护。

生产备份恢复到独立网络和临时数据库后，候选镜像完成完整应用启动迁移：

- 121张旧表按迁移前旧列计算的行数及内容指纹全部一致。
- 仅新增 `welfare_expense_category_tombstones（福利分类删除标记）`、`welfare_one_expense_categories（福利1分类）`、`welfare_two_expense_categories（福利2分类）`三张表。
- `reimbursements（报销）`和`monthly_financial_manual_items（月报手工项目）`各新增四个福利分类编号／名称快照字段；全部旧记录的新字段保持空值。
- 八项新增福利外键及一致性约束均已验证，六项新增索引均有效；仅原有两项历史付款日期约束继续保持未验证状态。
- 原70笔报销、13条月报手工项、2026年8月报表和第4版月结快照均未改写。
- 隔离查询和下载继续得到407-AI 12,103元、8H-AI 1,457.50元、茶歇426元、团建10,072.86元、体检0元，福利金合计24,059.36元；下载包含月度结算、手工项目明细、自动来源明细、校验四张工作表。

## 生产验收

- 生产应用、`PostgreSQL（关系数据库）`和`Nginx（反向代理）`均为健康状态；外部健康接口返回 `200` 和有效 `JSON（结构化数据）`。
- 活动首页已切换到 `index-DJLh7iop.js（当前入口脚本）`。入口、菜单、审批中心、分类管理和类型配置五个活动脚本均返回 `JavaScript（脚本）`内容类型，并包含福利2路由、统计、角标、分类接口和玫瑰色主题标识。
- 福利1、福利2分类接口未登录请求均返回 `401`，证明路由已注册且权限生效；福利1、福利2页面、审批中心和报销管理页面均返回当前单页应用入口。
- 生产迁移结果与隔离演练完全一致：福利1为六项规定顺序与启停状态，福利2为三项启用状态，墓碑为空，新增约束与索引全部有效。
- 121张旧表中，120张非会话表的旧列行数和内容指纹全部一致；`session（登录会话）`表行数仍为14，仅因应用重启和健康烟测发生会话内容变化。旧版全行指纹仅在新增空字段的报销表和月报手工表变化，旧列投影核对完全一致。
- 生产附件切换前后逐文件摘要完全一致。月度财务模板在源码与运行镜像中的摘要均为 `7f43e534613361cb748ce067b31c4a891bda703903ea7abbe843288a16501608`。
- 生产当前尚无董事长账号，因此未创建测试账号、测试报销或测试付款；上线验收未写入任何福利业务数据。

## 回滚

发布包中的 `rollback.yml（回滚覆盖配置）`固定指向切换前镜像 `yulilog-prod-yulilog:outflow-total-20260909`。如需回滚，只切换应用镜像并重载反向代理，保留生产数据库卷和生产附件；不得使用切换前数据库备份覆盖上线后的正常业务数据。新增福利表、空字段和约束属于向前兼容结构，可随旧应用保留。

## 更新日志

- 2026-09-09: 董事长双福利报销、分类安全删除、月报自动与手工合并及确认后的月度财务模板正式上线；隔离演练、生产迁移、活动资源、旧业务数据和附件验收通过。
