# 采购合同识别优化生产发布

2026-09-14 已按用户“进行上线”的指令完成发布。正式应用于北京时间 10:14:55 切换，正式域名健康接口和首页访问均正常。

## 发布内容

- 非主营采购首页短标题可在同页明确采购对象佐证下识别，无须附加“合同／协议”后缀；正文中的其他约定不再进入标题候选。
- “本合同所列产品的产品总金额”等明确整合同声明按总额识别，保留部分产品、分期比例、定金、尾款、单价及金额冲突拦截。
- 双主体及两个独立公章占位下方的唯一日期可作为落款日期；到达附件、交货等新业务段落即结束扫描。
- 解析版本为 `contract-structure-v21`（合同结构解析第二十一版）。本次没有新的接口、前端产物、依赖或数据库结构迁移。

## 产物与基线

只替换生产镜像中的 `dist/server/services/contractOcr.js`（合同识别模块），其余内容继承原生产镜像。修改前源码编译产物与原生产文件逐字节一致。

- 原镜像：`yulilog-prod-yulilog:reviewed-receipt-preview-20260913-233327`。
- 原镜像摘要：`sha256:27bd67f56baf71cd6050620e4a6e887b1cb355ec5b17b99287a13b72d9a7a662`。
- 新镜像：`yulilog-prod-yulilog:procurement-ocr-v21-20260914`。
- 新镜像摘要：`sha256:124aa30c0234226c43e251433ac2ab556e57dce865ee11b2ffedf3efda8917ce`。
- 新识别模块摘要：`114dc5f4732c05b9b170ba997dea7c0ca4b09281f830c0b4781dd64ebf772570`。
- 发布资料目录：`backup/procurement-ocr-release-20260914.upI60Z/`，包含数据库备份、原模块、编译结果、配置覆盖文件和验证日志。

## 验证结果

- 发布前完整类型检查通过；254 组、3531 项测试通过。首轮测试因沙盒不允许创建测试临时目录失败，在获准环境重跑后全部通过。
- 使用无网络临时容器运行候选生产镜像；只读验证原合同，不连接业务数据库。
- PDF（便携式文档格式）合同最终自动采用通过：项目名称“岗亭定制采购”、合同总额 13600.00 元、签订日期 2026-09-10，甲乙方正确，无阻断项、无漏识别页面。
- DOCX（开放文字处理文档）原件的项目名称与金额同样通过自动采用。日期在扁平表格文本中缺少可验证位置时仍按原有规则合法留空，不猜填。
- 生产启动后确认镜像、解析版本与识别模块摘要均与已验证产物一致，容器健康。
- 已校验并重载反向代理，识别接口等待配置保留；正式域名健康接口及首页均正常。
- 生产前后合同数 154、识别任务数 4、附件数 684 保持一致。逐字段比较仅发现原有启动核算逻辑将一份未识别草稿的 `current_effective_amount`（当前核算金额）由空值归一为 0.00；原始合同金额、名称、状态、识别结果和其余合同记录未改写。本次未自动重跑或消费旧合同上传记录。

## 回滚方式

在项目根目录执行以下命令可恢复原应用镜像；数据库和上传文件不恢复覆盖。

```bash
docker compose -f docker-compose.prod.yml -f backup/procurement-ocr-release-20260914.upI60Z/compose.rollback.yml up -d --no-deps --no-build --wait --wait-timeout 180 yulilog
bash scripts/reload-production-nginx.sh
```

当前生产覆盖文件为 `backup/procurement-ocr-release-20260914.upI60Z/compose.release.yml`。后续发布应先核对这一实际生产镜像基线，避免把其他未发布工作混入。
